# 版本更新记录（CHANGELOG）

记录各版本**用户可感知**的变更（新功能、行为变化、重要修复）；内部重构与工程改动不列入。
操作手册只描述当前行为，历史演进以此文件为准。

---

## v1.16.1（2026-08-27）

- **音频降噪落地**：降噪滑杆+「应用」恢复实效——libav 引擎原生**谱门控**降噪
  （STFT 噪声谱估计 + 增益掩码 + 时频平滑，零外部依赖）；只改善语谱图/音量
  曲线显示，播放音频与原始数据不变；调回 0 再应用即复原
- 修复：降噪 UI 自引擎换代后为空操作（滑杆值不参与分析），现已实装
- **播放音频降噪**：设置菜单新增开关（默认关）——流式谱门控 DSP 实时降噪
  （afftdn 式自适应底噪跟踪），强度与语谱图降噪滑杆共享；scrub 旁路、seek 自动
  重建；仅监听链路，源数据不动；1s 设备缓冲吸收计算滞后，稳态零音画偏移；
  多机窗口全部引擎同步生效
- **曲线图/语谱图滚轮操作归一化**：滚轮=X 轴缩放@鼠标位；Ctrl+滚轮=Y 轴缩放
  @鼠标位（曲线图新增亮度轴缩放，语谱图为频率轴）；Alt+滚轮=X 轴平移（两图新增）
- **修复：部分监控录像播放时声响比音量曲线/语谱图亮斑早出现（时有时无，实测约 2 秒）**——
  录像机写盘卡顿会在音频时间戳上留下空档；分析侧早已补静音对齐（曲线正确），
  播放侧此前把空档「压塌」导致空档后的声音提前。现播放引擎同步补偿：
  空档处补等长静音、重叠音频裁帧首，并加固声卡短写防护；声音、画面、光标、
  曲线四者时间轴重新严格一致
- **副屏全屏显示（多屏主机）**：「视图」菜单新增——把视频原画面送到指定屏幕
  全屏展示（菜单手选屏幕并记忆选择，`F11` 快捷用上次的屏）；纯画面无叠加，
  画面调节与主画面实时同步；`ESC`/双击退出，鼠标静止自动隐藏
- **崩溃黑匣子**：异常退出时自动保存诊断信息（minidump + 最后阶段记录）到
  本机诊断目录，下次启动提示位置——现场闪退可凭此反馈定位

## v1.16.0（2026-08-26）

- **证据快照分屏**：放大镜开启时，快照的视频帧改为左右分屏——左半原生全分辨率原帧（标注+金色源区域括号+OSD），右半放大视图同高（标注同步烧录），画幅双宽、细节零损失
- **软件内图片预览器**：案件树快照条目带缩略图，双击/右键「预览」打开——滚轮缩放（光标锚点）/拖动平移/双击复位/1:1/资源管理器定位；校时照片、证据帧等场景复用同一组件
- **多机「已对时」标识**：完成北京时间对时（直接或经同事件对时间接获得）的路，瓦片挂绿色「✓ 已对时」角标、合并时间线行标签下挂「✓ 已对时·北京时间」
- **同事件对时沙盒双播放钮**：「▶ 播放选中瓦片」（单路逐帧比对）/「▶▶ 播放全部」（配合双路听音）
- **多机窗口默认最大化**；多机每次起播前自动拦停主视口播放（防双路音频/资源竞争）
- 修复：多机时间线游标时刻气泡被窗口顶裁掉不可见；刻度标签首末半字裁切
- 修复：案件树删除快照误判「报告索引越界」（极端情况下可能误删报告文件）
- 修复：关于框/启动画面版本号滞后；此后全部版本号以 CMake 单一真源自动同步

## v1.15.3（2026-08-25）

- 同事件对时打磨：保存按钮分步守卫+步骤指引；保存后主视口立刻同步新校时；确认卡大白话（「目标的钟慢 2 分 14 秒」）；「应用预览」即真实替换播放模型（所见即所得）；速率行改行人话；合并轨行名显示机位标签
- 导出选段：默认恒原速（同选段且上次有变速时醒目提示，不再静默沿用）
- 显著性闸门收紧：速率偏差 ≥10 秒/天才报漂移（原 30 秒/天）
- 修复：连环弹窗（该机器 QMessageBox 关不掉，全面改非模态）；导出完成弹窗取消（面板结果条+📂+状态栏）；导出 OSD 移除「演示副本」默认文案
- 修复：DVR 抽帧导出文件选段导出画面冻结（pkt 时间戳归一化）

## v1.15.0 ~ v1.15.2（2026-08-24）

- **机位组正式化**：案件检材按「机位组」组织（C01/C02… 机位编号创建分配、永不复用、改名不动）；案件树按组分层；前处理产物登记时询问归组；点位图/报告/多机时间线全系统引用机位组；老案件打开自动迁移
- 机位独立编号 C##（与文件编号 V##/P## 双体系：C##=摄像头身份，V##/P##=文件身份）
- 点位图标注改机位编号、文件编号两位化
- 报告「时间校准」节白话化（读数以修正后值为准，OCR 原文留档标注）

## v1.14.0（2026-08-23）

- **生成分析报告**：案件菜单一键生成 DOCX 报告草稿——生成前自检（阻断项/提示项）+ 信息补录（审核/批准/送检人、逐路拍摄方向/提取方式）；自动填入检材清单（MD5+SHA-256 双哈希）、时间校准表、校准证据截图、接力对时取证链、火势时间表（来自图表标签）、亮度曲线图、快照附件；人工撰写位留白
- **监控点位图编辑器**：导入底图（裁切对话框）、机位拖放布点、滚轮调朝向/张角/字号、标准图框出图（PNG 存案内），报告自动嵌入
- 前处理拼接记录列为报告证据（产物↔来源全表 + operations.log 关键决策）

## v1.13.0 ~ v1.13.3（2026-08-22）

- **导出选段视频**：A/B 区间合成 1080p MP4（画面+亮度曲线+语谱图+移动游标）；分段变速（标签自动为分速边界，逐段 0.25x~8x）；标签/放大镜子窗/信息角标入导出；非模态导出面板；完成后一键定位产物
- **多机合并轨**：同机位多文件并成一路播放（自动跨段换文件，重叠区斜纹标 ⚠）
- **同事件对时重做引导**：四段式面板（选基准→选目标→打「同一瞬间」标记→预览保存），顶部横幅永远显示"现在该干嘛"；取证链确认卡；多跳接力、成环禁止
- 多机选段导出：全机位宫格+覆盖条+游标 MP4
- 放大镜内嵌主视口同一行（左右分屏、比例记忆）

## v1.12.3 ~ v1.12.9（2026-08-21）

- **校时体系重做**：对真实时间三方式（校时照片双框框选 OCR【推荐】/手动两时间/直接偏移量）；确认卡逐字校对；校时证据随案存档
- **时间重建**：变速/抽帧文件自动识别 → 分段直线模型（粗采样→分段检测→边界加密→音频校验），异常测点自动剔除标 ⚠
- 时间缺口显示：红虚线+「缺 Xh」标注，seek 自动跳过缺口；与刻度重叠时自动让位
- 多机：北京时间偏移全程生效（时间线/OSD/游标统一口径）；画面调节逐机位独立浮窗
- 前处理分段校时继承（.lumencal.json）

## v1.10（2026-08-20）

- **多机同步播放**独立大窗口：机位勾选清单（校时状态实时读取）、合并时间线模式（墙钟色块/缺口/游标走带）、2 路临时对齐（会话级不落盘）、逐瓦片放大镜、性能治理（预览降清档）

## v1.8.0（2026-08-17）

- **分析引擎换代**：亮度/音量/语谱分析切换为内置 libav 原生引擎（进程内、全帧率逐帧、取消抽样上限，47 分钟 1440p 约 40 秒）；Python 分析引擎退役（Python 运行时仍随包提供，供时间戳 OCR 使用）

## v1.7.0（2026-08-18）

- **前处理 v2**：硬件编码可选（NVENC/QSV 自动探测）；时间重叠自动修剪（默认开，报告留痕）；多组并行转码；转码产物自动命名（通道_日期_起止时间）；跨相机混拼分辨率归一

## 更早版本（摘要）

- **v1.3**：案件管理（案件目录、指纹校验、移交包、独立/案件双模式）
- **v1.1**：`.vla` 存储升级为 VLA2 二进制容器（单文件自包含、频谱内嵌、体积缩小 5~10 倍；旧格式可继续读取）
- **v1.0**：首个正式发布版（亮度量化分析、音频分析、截图叠加、A/B 循环、时间戳提取、校时雏形）

---

> 更细粒度的开发记录见源码仓库 `HANDOVER.md`（批次级）与 git 提交历史（逐次级）。

## 未发布

### 新增（账号与反馈系统 v1.2）
- 「帮助 → 账号管理」：当前账号展示（手机号打码）+ 修改姓名/单位（需当前手机号短信
  验证码二次验证，服务端双校验防越权）+ 退出登录；邀请码账号修改区灰置（提供方管理）
- 署名自动接入：新建/编辑案件的「调查员/单位」自动预填登录账号姓名/单位（可改），
  报告「分析人」落款与点位图「制图」栏经案件信息自动贯通
- 服务端新增 updateProfile 函数（token+access_token 双校验、invite 禁改、手机号一致性）

### 新增（账号与反馈系统 v1.1）
- 登录窗改为主界面弹出后模态覆盖（先见主窗再登录）；取消登录即退出
- 两层注册流：第一层只填手机号+验证码；仅未注册用户展开第二层补填姓名/单位
  （提示语：将自动用于案件录入和分析报告生成）；老用户登录零填表
- 邀请码通道完善：响应补 expires_at；邀请码用户不适用 30 天在线复核（180 天 token 为准）；
  心跳新增账号停用检查（管理端 users.status=disabled 即踢）
- 反馈提交自动携带姓名/版本/平台；服务端 name 从 users 表兜底
- 服务端三函数更新部署；docs/cloudbase/README 新增「邀请码管理指南」（生成/发放/防滥用/吊销）
- 手册新增「账号登录」节（含邀请码离线使用说明）

### 新增（账号与反馈系统 v1）
- 启动登录闸：手机号+短信验证码注册/登录、邀请码通道（离线机器），强制登录后使用
- 凭证本地存储 + 30 天策略：token 30 天有效、距上次在线验证 ≥30 天强制重登；断网宽限内放行、超期锁定
- 启动后异步心跳：自动续签（距到期 <7 天）、token 失效/账号停用拦回登录框
- 帮助菜单新增「意见反馈」：可勾附带诊断信息（版本/系统/上次异常退出；绝不含案件数据）
- HTTPS 走 Qt Schannel 后端（Windows 系统 TLS，零新增 DLL）；打包清单新增 tls/qschannelbackend.dll
- 服务端：腾讯云开发 CloudBase 四云函数（authRegister/authHeartbeat/inviteActivate/feedback）+ 三集合，CLI 一键重部署
