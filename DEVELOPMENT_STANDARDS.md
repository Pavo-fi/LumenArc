# LumenArc 开发规范

> 本文档是 LumenArc v1.0 起所有开发活动的基本遵循。任何提交违反本文档的"红线"条目，评审时直接打回，不接受"先合入后整改"。

---

## 一、产品第一原则

LumenArc 是**火灾调查取证工具**。它的一切技术决策服从以下优先级排序：

1. **结果可信** —— 界面显示、时间轴、导出数据三者永远一致。"看起来正常但结果是错的"是本项目最高严重等级的缺陷，优先级高于任何崩溃。
2. **数据不丢** —— 用户的 ROI、标签、辅助线、分析结果在任何操作路径下不得静默丢失。
3. **长时间稳定** —— 火调现场是几小时连续作业，内存、句柄、队列必须有界。
4. **可维护** —— 人员会轮换，代码必须让新人一周内能独立修改一个功能。

---

## 二、架构红线

### 2.1 分层与依赖方向

```
┌─────────────────────────────────────────────┐
│ ui/          纯展示组件，不持有业务状态          │
├─────────────────────────────────────────────┤
│ app/         应用服务层：流程编排、状态机        │
├─────────────────────────────────────────────┤
│ domain/      纯数据模型，不依赖 Qt Widgets     │
├─────────────────────────────────────────────┤
│ infrastructure/  引擎实现（VLC/FFmpeg/Python） │
└─────────────────────────────────────────────┘
依赖只允许向下。
```

- **R1** 依赖方向不可逆。`domain` 禁止 include 任何 Qt Widgets 头文件；`infrastructure` 禁止知道 ui 的存在；`ui` 与引擎之间只通过接口通信。
- **R2** 禁止向下转型。UI/app 层出现 `qobject_cast` 到具体引擎类 = 接口设计缺陷，先把能力上移到接口再使用。
- **R3** 禁止穿透封装。不允许 `m_chartPanel->axisX()->setRange(...)` 这类伸手进组件内部的操作。需要什么能力，在组件的公开接口上加方法。
- **R4** 引擎中立。ui 层和 app 层代码中不得出现 VLC / FFmpeg / Python 字样（infrastructure 接口层除外）。新增引擎只需实现接口并注册，不得修改任何调用方代码。

### 2.2 状态管理

- **R5** 单一数据源（SSOT）。任何状态只允许一个 owner，其余组件订阅或派生。新增成员变量前必须先回答："这份状态的真身在哪里？"如果答不上来，不允许加。
- **R6** 跨组件共享状态必须经过 domain 模型或 app 服务，禁止两个 UI 组件互相直接读写对方内部状态。
- **R7** 流程性状态（分析、加载、切换）必须用显式状态机表达，禁止用多个布尔/枚举成员拼装隐式状态。

### 2.3 扩展方式

- **R8** 新分析功能必须以"任务描述 + 结果 channel + 展示面板"的形式注册进任务框架。禁止在 MainWindow 或 AnalysisController 里加 `if (taskType == ...)` 分支。
- **R9** 复制粘贴即负债。同一段逻辑需要第二份时，先抽函数；发现第三份时，必须做设计评审。禁止"先复制，以后再说"。
- **R10** 死代码零容忍。未接线的函数、恒空指针、被替换的旧实现，发现即删。git 历史就是备份，不需要"留着参考"。

---

## 三、正确性规范

- **C1** 错误用类型表达，不用字符串。取消、失败、超时必须用独立的信号或错误码区分，禁止比较错误文案。
- **C2** 静默失败 = bug。文件写入返回值、进程退出码、格式解析结果必须检查；失败必须让用户可见或写入日志。
- **C3** 涉及时间轴、坐标映射、ID 匹配、格式解析的代码，按"错误数据可能流入取证报告"的严重等级评审和测试。
- **C4** 跨进程/线程边界必须有背压和超时：凡是有界队列连接生产者消费者必须设容量上限与丢弃策略；凡是同步等待外部进程必须有超时，且不得阻塞 GUI 线程超过 100ms。
- **C5** 资源（内存、句柄、缓存）必须有界。任何"每打开一个 X 就保留一份 Y"的缓存必须有容量上限和淘汰策略。
- **C6** 数值精度：导出到文件/CSV 的浮点数必须显式指定有效位数；时间值内部一律 qint64 毫秒，禁止 float 时间在各模块间传递。

---

## 四、文件格式规范（.vla / .spec / CSV）

- **F1** 任何落盘格式必须带：格式标识（魔数或 version 字段）+ 版本号 + 加载时的版本上下限检查。
- **F2** 格式变更 = 版本号 +1 + 显式迁移函数 + 迁移测试（旧文件加载后做字段级断言）。三件套缺一不可。
- **F3** 格式演进只加字段、不改语义。必须改语义时涨主版本并让旧版本明确报错，而不是静默错解析。
- **F4** 加载新版本文件时：高于支持上限的版本必须明确提示"文件由更新版本创建"，未知 channel/字段忽略并提示，禁止静默忽略。
- **F5** 格式相关的三处必须同步：写入代码、加载代码、格式注释/文档。注释写错版本号视同代码 bug。
- **F6** CSV 导出必须做引号转义（字段含逗号、引号、换行时用双引号包裹，内部引号双写）。

---

## 五、C++/Qt 代码规范

- **Q1** 信号槽跨线程时显式指定连接类型；引擎工作线程禁止直接触摸 GUI 对象，一律经 `Qt::QueuedConnection` 回 UI 线程。
- **Q2** 大对象（QImage、QVector 序列）传递利用隐式共享，禁止无必要的 `.copy()` / 深拷贝；每帧热路径上的拷贝必须有注释说明必要性。
- **Q3** `paintEvent` / mouseMove 等高频路径中禁止：堆分配大对象、全量重建 graphics item、对完整数据集的拷贝遍历。应复用对象、增量更新。
- **Q4** QObject 有 parent 的对象不手动 delete；无 parent 的对话框/窗口必须设 `Qt::WA_DeleteOnClose`。
- **Q5** 快捷键一律用 QAction/QShortcut 注册，禁止在 eventFilter 里写大 switch 路由键位。快捷键定义、菜单文案、tooltip、手册四处必须一致。
- **Q6** 注释与代码同步修改。注释描述与实现不符视同 bug。版本号、格式说明、行为描述类注释是重点。
- **Q7** 头文件注释保留 `@file/@brief/@author/@date/@version` 块；修改文件时更新 `@version`。
- **Q8** 命名：类用 UpperCamelCase，成员变量 `m_` 前缀，函数 lowerCamelCase，与现有代码保持一致。

---

## 六、Python 分析脚本规范

- **P1** 脚本与 C++ 引擎的协议（stdout 单行 JSON / stderr `PROGRESS:`/`ERROR:`/`WARNING:` 前缀行 / 二进制旁车文件传大数据）不得擅自变更；变更必须双端同步 + 版本协商字段。
- **P2** 多视频处理时，每个视频的 fps/时长必须各自取值，禁止复用第一个视频的参数。
- **P3** 大数据（频谱等）走二进制旁车文件，禁止塞进 JSON；浮点用 float32 除非有明确精度需求。
- **P4** 临时文件必须登记清理（atexit + finally），进程崩溃不得残留。
- **P5** 发布包中的脚本与仓库根目录脚本必须保持单一来源，由构建脚本拷贝，禁止手工维护两份。

---

## 七、流程规范

### 7.1 修 bug 的标准顺序

1. 先写复现（自动化测试，或精确到点击步骤的最小操作序列）
2. 修复
3. 验证复现不再触发
4. 不接受"我改了应该好了"

### 7.2 提交纪律

- **T1** 重构与行为变更不得在同一提交。纯移动/重命名的提交不允许夹带行为修改（保证 `git bisect` 可用）。
- **T2** 提交信息格式：`<type>: <一句话说明>`，type ∈ `feat / fix / refactor / perf / docs / test / chore`。参照现有 git log 风格。
- **T3** 一个提交只做一件事。评审者无法一句话说清的提交必须拆分。

### 7.3 PR 自检清单（逐项勾选后提交评审）

- [ ] 新增状态的唯一数据源在哪里？
- [ ] 每个失败路径，用户能看到什么？
- [ ] 三个边界场景测过吗：长视频（>1h）、多视频、无音轨视频？
- [ ] 涉及的 .vla 格式 / 快捷键 / 操作手册 / README 同步了吗？
- [ ] 有没有复制粘贴现有代码？为什么没抽函数？
- [ ] 有没有新的静默失败点（未检查的返回值、被吞的异常）？

### 7.4 测试策略（务实排序，不追求覆盖率数字）

1. domain 纯逻辑：序列化往返、抽稀算法、RoiModel ID 唯一性 —— 用 Qt Test
2. Python 分析脚本：不同 fps 拼接、损坏文件、无音轨、多边形掩码 —— 用 pytest
3. 状态机对抗测试：引擎异常退出、不发信号退出、取消竞态
4. UI 层不做自动化，维护一份**发版前手工点检清单**（约 20 项核心路径），随功能演进更新，发版前必跑

### 7.5 文档同步

- 功能变更必须同步：MANUAL.md、README.md 快捷键表、应用内快捷键速查对话框、PDF 手册（发版时）。
- 架构决策（为什么这么设计）写入 PROJECT_LOG.md，代码注释只写"是什么"，不写"为什么是"。

---

## 八、当前已知技术债清单（禁止新增同类）

| 债项 | 状态 |
|---|---|
| MainWindow 上帝类（2700 行） | P2 拆分，新功能禁止再往里加逻辑 |
| AnalysisPhase 硬编码两阶段枚举 | P1a 任务化状态机替代 |
| AnalysisSnapshot 硬编码 luminance/audio 成员 | P1b 通道化改造 |
| RegionModel/PolygonModel 双模型复制 | P1b 合并为 RoiModel |
| 旧版 SpectrogramPanel（死代码） | P2 删除 |
| i18n 为 lang(zh,en) 硬编码二选一 | 维持现状，新增字符串沿用现有方式；新增语言需求时整体迁移 tr() |

---

*本规范自 LumenArc v1.0 起生效。修订本规范需要团队评审并在 PROJECT_LOG.md 中记录决策理由。*
