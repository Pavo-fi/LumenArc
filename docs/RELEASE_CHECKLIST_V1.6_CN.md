# v1.6.0 手工点检清单（RELEASE_CHECKLIST）—— GPU 显示管线 Stage 1（P-29）节

> **背景**：P-29 Stage 1 把视频**上屏缩放**从 CPU 逐像素重采样改为 GPU 纹理
> 缩放（`GlVideoSurface`，QOpenGLWidget）。帧语义（旋转/亮度对比/快照融合/
> 放大镜/ROI）全部不变——GPU 只负责"把已处理好的图放大画出来"。
> 自动化已覆盖：offscreen GL 失败回退全链不崩 + 数据通道逐位一致
> （ui_chain 场景 0d）。**性能收益与 HiDPI/RDP 行为必须真机验证**。
>
> 本文件为 v1.6.0 点检清单（首节=P-29 GPU Stage 1）；v1.6.0 其余 P-xx 完成时续节。
> 自动回归（19 套绿）不替代本清单。

## 0. 前置（一次性准备）

| # | 项 | 操作 | 结果 |
|---|----|------|------|
| 0a | 1440p 验收素材 | 明景重导出一份 1440p H.264 片段（≥30s，运动内容多） | ☐ |
| 0b | 基线对照（CPU） | `设置 → 视频渲染 → CPU 软件渲染`，快拖素材 30s，记下控制台 `scrub 2s:` 行的 **uiDrops**（应频发 >0） | ☐ |
| 0c | 控制台启动 | 从 cmd 直接运行 exe（`qDebug` 日志走控制台），快拖时观察 `scrub 2s: displays=… cacheHits=… reseeks=… uiDrops=… maxGap=…ms` | ☐ |

## 1. 性能验收（方案 §6.1，硬指标）

| # | 项 | 操作与验收线 | 结果 |
|---|----|------------|------|
| 1a | **uiDrops 归零** | `视频渲染 → 自动`（GPU 生效），1440p 素材快速来回拖进度条连续 ≥30s：控制台 `scrub 2s:` 的 **uiDrops=0**、maxGap≤40ms；对照 0b 基线应有数量级下降 | ☐ |
| 1b | 播放 CPU 占用 | 1440p 正常播放（不拖），任务管理器看进程 CPU：GPU 模式应明显低于 0b 基线（paint 从 ~10ms/帧 降到 <2ms 量级） | ☐ |
| 1c | 4K 冗余（如手边有 4K 素材） | 播放/拖拽不 worse-than-CPU（无硬指标，手感不卡即可） | ☐ |

## 2. 功能回归（方案 §6.2 点检项，逐项点）

> 每一项都在 **GPU 模式（自动/强制 GPU）** 下点；确认与 CPU 模式表现一致。

| # | 点检项 | 预期 | 结果 |
|---|--------|------|------|
| 2a | 放大镜 | 开/关、标识框+倍率徽章、拖动跟随，无残影/撕裂 | ☐ |
| 2b | 钉图（P 键或按钮） | 钉住的帧显示正常，移动窗口/放大后钉图不错位 | ☐ |
| 2c | **截图融合（opacity 拖动实时）** | 设截图融合后**拖动不透明度滑杆**：视频上叠加的透明度**跟手实时变化**（GL 与 CPU 路径一致，本次修复点 P1-1） | ☐ |
| 2d | 证据快照（S 键） | 快照内容与**屏幕所见一致**（含融合/框选），无多一层/少一层 | ☐ |
| 2e | ROI 矩形 + 多边形 | 拖拽顶点/整块移动，命中与跟随正常 | ☐ |
| 2f | 辅助线 / 时间戳框选 | 拖拽、双击落点正常（校时 UX 不变） | ☐ |
| 2g | 显示旋转四档 | 90/180/270 画面方向正确，进度/帧步进不受影响 | ☐ |
| 2h | 画面调节 LUT | 暂停态拖亮度/对比滑杆**实时预览**，放大镜内同步 | ☐ |
| 2i | 加载卡片 / 品牌空态 | 开视频瞬间"导入中…"卡片、无视频时 logo 水印正常显示（raster 层，压 GL 之上） | ☐ |
| 2j | 窗口拉伸 / 分屏切换 | QSplitter 拖动、窗口缩放：视频画面 letterbox 比例正确，**不出现画面缩到一角/比例失调**（HiDPI 修复点 P1-2 的 1:1 形态） | ☐ |
| 2k | **多显示器与 DPI**（125%/150% 缩放机） | 视频画面填满正确区域、比例正常（无 1/dpr² 缩角）；跨屏拖动窗口正常 | ☐ |
| 2l | **RDP 远程会话**（如可测） | 远程桌面里打开：应自动回退 CPU 软件渲染，画面照常（不黑屏/不崩） | ☐ |
| 2m | CPU/GPU 开关往返 | `视频渲染` 三态来回切：画面即时切换无花屏；`强制 GPU` 在 GL 不可用时弹提示且画面照常（CPU） | ☐ |

## 3. 判定

- **1a 是硬指标**：uiDrops 不归零 → Stage 1 性能目标未达成，回炉分析
  （可能需调纹理格式/上传路径，方案 §5.3 留有后手）。
- 2 组任一项与 CPU 模式表现不一致 → 记为缺陷，修复后重跑本清单。
- 全部通过 → 在 `docs/PENDING.md` 勾销 P-29（Stage 1 完成），v1.6.0 总清单并入本文件。