# 火灾调查视频分析软件
追光者 Lumen Arc v1.3 — 操作手册

---
本项目由广东省火调技术中心发布并维护，采用 Apache 许可证 2.0 版进行授权。
---

## 一、软件概况

**追光者 Lumen Arc** 是一款面向火灾调查的视频分析桌面工具。它可以对视频画面中的指定区域逐帧计算平均亮度，绘制亮度随时间变化的折线图；同时支持独立音频分析（音量曲线 + 语谱图）、截图叠加、A/B 循环、辅助线标注等多种功能，帮助你从视频和音频两个维度提取关键证据。

### 运行环境要求

| | 最低配置 | 推荐配置 |
|---|---|---|
| 操作系统 | Windows 10 1809+ 64位 | Windows 10 22H2 / Windows 11 64位 |
| CPU | 4 核（i5-6500 / Ryzen 3 1200 级） | 6 核 12 线程以上（i5-12400 / Ryzen 5 5600 级） |
| 显卡 | 任意支持 D3D11 的核显（Intel HD 4000+） | Intel UHD 620+ / 任意近代核显或独显 |
| 内存 | 8 GB | 16 GB |
| 存储 | 机械硬盘可读，素材建议放 SSD | NVMe / SATA SSD 存放素材 |
| 显示器 | 1920×1080 | 2560×1440 或以上 |

- 本软件**不吃显卡**：视频解码使用核显 D3D11VA（H.264 为近十余年 GPU 标配），画面绘制为 CPU 路径，预算优先给 CPU 和 SSD。
- 性能瓶颈实测排序：素材在机械盘的冷 seek > CPU 软解吞吐 > 内存。长时间拼接视频（如 2 小时 10GB）的快速拖拽对随机读取敏感，放 SSD 上手感差异明显。
- 最低配置可流畅分析 1080p 素材、播放 1440p 素材；推荐配置针对 1440p 监控素材的快拖观测（实测解码吞吐 ≈31 倍速）与长视频分析。

### 主要功能一览

| 功能       | 说明                              |
| -------- | ------------------------------- |
| 亮度量化分析   | 框选矩形或多边形 ROI，逐帧计算区域平均亮度并绘制曲线    |
| 音频分析     | 无需 ROI，直接提取音轨并生成音量曲线和语谱图        |
| 语谱图 + 降噪 | 可视化音频频谱，支持频谱门控降噪                |
| 多视频分析    | 视频列表支持添加多个视频，并可快速复制已有 ROI 区域并粘贴 |
| 放大镜      | 局部放大查看视频细节，支持在放大镜内绘制和调整 ROI     |
| 截图叠加     | 截取视频帧或加载外部图片，半透明叠加到当前视频         |
| 时间戳提取    | 固定一个区域，实时显示在独立浮动窗口中             |
| 视频校时     | 图表时间轴对应真实墙钟：三点自动识别（含漂移修正）/流内录制时间/手动/北京时间校验 |
| 轴标签      | 在折线图上添加文字标记，标注关键时刻              |
| A/B 区域循环 | 在图表上设置 A/B 两点，两点之间循环播放          |
| 辅助线      | 在视频画面上绘制水平、垂直或斜向参考线             |
| 分析结果共享   | 分析成果独立缓存，支持跨终端传递                |

### 启动与打开视频

双击 `LumenArc.exe` 启动程序。打开视频的方式有三种：

| 方式 | 操作 |
|------|------|
| 菜单打开 | `文件 → 打开视频`，或按 `Ctrl+O` |
| 拖放打开 | 直接把视频文件拖入程序窗口 |
| 加载历史分析 | `文件 → 加载分析结果`，或按 `Ctrl+L` |
| 自动加载缓存 | 打开视频时如果同目录有已保存的 `.vla` 分析结果，会提示是否直接加载，实现**分析结果通过 vla 文件跨设备传递** |

支持格式：MP4、AVI、MKV、MOV、WMV、FLV、WEBM 等常见格式。

> **播放内核说明**：采用自研 FFmpeg 内核，对监控导出的“伪 MP4”（实际为 MPEG-PS/TS 流）提供精确到单帧的进度拖拽和逐帧步进。支持 D3D11VA 硬件解码（`设置` 菜单可开关、可指定适配器，重启生效），拖拽时自动切换多线程软解追赶保证落点精确。倍速播放时音频变速（音调随速度变化），拖拽进度时播放与光标对齐的短音频片段。

> **分析引擎说明（v1.8.0）**：亮度/音量/语谱分析使用内置 libav 原生引擎（进程内解码，**全帧率**逐帧分析——不再有 5000 帧抽样上限，47 分钟 1440p 全帧率约 40 秒完成；亮度数值与历史 Python 引擎一致（容差 ±1），语谱底噪更干净）。v1.8.0 起 Python 分析引擎退役（用户拍板 2026-08-17），`设置 → 分析引擎` 切换菜单移除；内置 Python 运行时仍随包提供（时间戳 OCR 与后续报告功能使用）。

### 界面布局

程序主界面分为几个主要区域：

- **左侧**：视频列表面板（可收起），用于管理多个视频
- **中间**：视频播放区 + 折线图/语谱图面板
- **右上**：放大镜面板（滚动鼠标滚轮自动出现；**v1.13.1 起内嵌视频同一行**——左半原始画面、右半放大镜等大同高并列，分割条可拖比例、关闭后再开记住比例；图表/语谱图保持全宽不受挤压；左侧视频列表/案件列表为全高栏，各自头部有常驻 ◀/▶ 手动折叠钮，放大镜打开时仍会自动收成细条、关闭自动还原）
- **顶部**：工具栏，包含播放控制、分析按钮、ROI 模式等
- **底部**：状态栏，显示分析进度和操作反馈

「画面调节」面板（v1.12.9 起）为**独立浮窗**呼出（不挤占 dock 布局，可拖到任意位置，关闭即隐藏），与多机同步页同款。

### 中英双语

通过菜单 `帮助 → 语言` 切换中文 / English，切换后程序自动重启。

---

## 二、视频播放与控制

| 操作      | 方式                                                |
| ------- | ------------------------------------------------- |
| 播放 / 暂停 | 按 `空格` 或 `k`，或点击工具栏播放/暂停按钮                        |
| 前进一帧    | 按 `→`                                             |
| 后退一帧    | 按 `←`                                             |
| 音量增大    | 按 `↑`                                             |
| 音量减小    | 按 `↓`                                             |
| 倍速播放    | 点击工具栏倍速按钮，循环切换 `0.25x → 0.5x → 1x → 2x → 4x → 8x`（FFmpeg 内核倍速时音频变速，音调随速度变化） |
| 停止播放    | 点击停止按钮（倍速自动重置为 1x）                                |
| 加速一档    | 按 `c` 或 `l`                                       |
| 减速一档    | 按 `x` 或 `j`                                       |
| 恢复 1x   | 按 `z`                                             |
| 帧级跳转    | 拖拽折线图中的橙色竖线，视频实时同步到对应位置                           |

> 播放按钮（播放、暂停、停止、倍速、分析、校时、截取）在未打开视频时为灰色禁用状态，打开视频后自动启用。

### 画面调节（亮度 / 对比度 / 旋转）

点击工具栏**「画面调节」**按钮打开调节面板（停靠右侧，点 X 关闭，按钮态同步）。

| 操作 | 方式 |
|------|------|
| 亮度 / 对比度 | 拖滑杆（±50），暂停时拖滑杆也实时预览 |
| 伽马 | 0.30 ~ 3.00（1.00 = 不变）；>1 提亮暗部/中间调而不冲淡高光，夜间/昏暗监控素材常用 |
| 黑点 / 白点 | 色阶拉伸：黑点以下裁为纯黑、白点以上拉为纯白，应对烟雾/雾低对比素材 |
| 反色 | 勾选后画面转为负片（某些高光场景更易辨认细节） |
| 旋转 | 点「⟳ 顺时针 90°」按钮，按 0° → 90° → 180° → 270° 循环 |
| 复位 | 一键全部回默认 |

**要点**：
- 调节只影响**显示与截图快照**；亮度分析、ROI 数据、语谱图与证据文件始终保持原始画面不变。
- 主画面、**放大镜**、时间戳钉图（Ctrl+Shift 框选）三处视图应用同一组调节，所见一致。
- 旋转时 ROI、多边形、辅助线、时间戳框选、放大镜、截图叠加**随画面一起转**；新画的框自动换算回原视频坐标存储与分析，证据数据始终对应同一物理位置。
- 每个视频的记忆独立：切换视频后各自的调节与旋转档位自动恢复。
- 证据快照（工具栏「快照」或快捷键 `S`）为所见即所得：保存的画面即当前调节/旋转后的显示效果；旋转档位会写入 PNG 元数据并在保存提示中注明。快照内容详见本章末节「证据快照」。

### 证据快照（工具栏「快照」/ 快捷键 `S`）

一键把当前分析现场合成为一张竖向全宽 PNG（自上而下，有几段拼几段，黑底无留白）：

| 段落 | 内容 |
|------|------|
| 视频帧 | 所见即所得（旋转+调节后画面）；烧录 ROI 矩形/多边形/辅助线（按模型原色+序号标签）；左下 OSD 烧录文件名、当前帧标签（±1s 内最近）、北京时间（已校时）或相对时刻。**v1.16.0 起：开启放大镜时视频帧改为左右分屏**（与主页面观感一致）——左半原生全分辨率原帧+金色四角括号标出源区域，右半放大视图同高（标注同步烧录进放大坐标系，不另烧注记——左半 OSD 已有时间信息）；画幅=左宽+右宽（细节零损失） |
| 亮度+音量曲线 | 有分析数据时合成；离屏矢量重渲染为全宽横幅（曲线锐利不压扁），含橙色时间光标 |
| 语谱图 | 有音频分析时合成；CPU 全幅光栅化（与屏幕同一色标/视窗），含时间光标 |
| 放大镜视图 | 仅当放大镜开启且**未**走分屏（无有效源区域）时单列；分屏模式下已融入右半不再单列 |

各段之间有金色竖条分段标题，黑底无留白。

**保存位置**：打开案件时存 `案件目录/snapshots/`（案件面板快照组即时可见）；未开案件存视频同目录 `snapshots/`。文件名 `视频名_校准后北京时间.png`（未校时为 `视频名_tHH-MM-SS.png`），重名自动避让。

**软件内预览（v1.16.0）**：案件树快照条目带缩略图；双击（或右键「预览」）打开图片预览窗口——打开即自适应窗口；滚轮缩放（光标锚点）/拖动平移/双击复位/「1:1」「在资源管理器中显示」。该预览器为通用组件（imagepreviewdialog），校时照片、证据帧等图片均可复用。

---

## 三、视频列表与多视频分析

左侧的**视频列表**面板用于管理多个视频文件。

| 操作 | 方式 |
|------|------|
| 添加视频 | 点击面板底部「添加」按钮，或直接把视频拖入面板 |
| 移除视频 | 选中视频后点击「移除」 |
| 清空列表 | 点击「清空」 |
| 调整顺序 | 在列表中拖拽视频项 |
| 切换当前视频 | 双击列表中的视频 |
| 收起/展开面板 | 点击面板标题栏的箭头 |


---

## 四、ROI 区域与辅助线模式

工具栏上有三个互斥的模式按钮：

| 按钮      | 功能                       |
| ------- | ------------------------ |
| **矩形**  | 矩形 ROI 模式，在视频上拖拽绘制矩形分析区域 |
| **多边形** | 多边形 ROI 模式，单击添加顶点，双击闭合   |
| **辅助线** | 辅助线模式，绘制参考线（不参与亮度分析）     |

### 矩形 ROI 模式

| 操作   | 方式                             |
| ---- | ------------------------------ |
| 绘制区域 | 在视频画面上按住鼠标左键拖拽                 |
| 选中区域 | 点击已有区域（选中后边框变亮，四角出现白色调整点）      |
| 移动区域 | 选中后拖拽区域体（多边形ROI也只能在矩形模式下拖拽移动）  |
| 调整大小 | 选中后拖拽四角的白色调整点                  |
| 删除区域 | 选中后按 `Delete`，或右键点击区域          |
| 复制区域 | 选中后按 `Ctrl+Shift+C` 或点击「复制ROI」 |
| 粘贴区域 | 点击「粘贴ROI」或按 `Ctrl+Shift+V`     |

- 支持多个 ROI，每个自动分配不同颜色（赤、橙、黄、绿、青、蓝、紫），标记为 R1、R2、R3...
- 重叠区域点击时选中的是最上层的区域。

**数据保护：** 如果已经运行过分析产生了数据，此时再调整 ROI 区域，会弹出确认提示："调整该区域将导致亮度量化数据失效。确定要继续吗？"

- 点"确定"：清除已有分析数据，允许调整
- 点"取消"：不调整，恢复区域原位

### 多边形 ROI 模式

按 `P` 或在工具栏点击「多边形」进入多边形模式：

| 操作    | 说明                    |
| ----- | --------------------- |
| 单击    | 添加一个顶点                |
| 双击    | 闭合多边形（至少 3 个顶点）       |
| 右键    | 取消当前绘制                |
| 删除区域  | 选中后按 `Delete`，或右键点击区域 |
| `Esc` | 退出多边形模式               |

多边形 ROI 支持亮度分析，计算多边形内部区域的平均亮度。

### 辅助线模式

按 `G` 或在工具栏点击「辅助线」进入辅助线模式：

| 操作           | 说明           |
| ------------ | ------------ |
| 左键拖拽         | 绘制辅助线        |
| `Shift` + 拖拽 | 约束为水平或垂直线    |
| 左键点击线体       | 选中并可移动整条线    |
| 左键拖拽端点       | 调整端点位置       |
| 右键点击         | 删除辅助线（无需先选中） |
| `Delete`     | 删除选中的辅助线     |
| `Esc`        | 退出辅助线模式      |

辅助线用于在视频画面上标注参考线，不参与亮度分析。


---

## 五、亮度量化分析

### 运行分析

点击工具栏蓝色 **"亮度分析"** 按钮，程序会自动逐帧扫描视频，计算每个 ROI 的平均亮度。

| 状态   | 说明                                                         |
| ---- | ---------------------------------------------------------- |
| 前置条件 | 已打开视频 + 至少画了 1 个 ROI                                       |
| 运行环境 | Release 包已内置 Python 和 ffmpeg，无需手动安装；本地源码开发版需自行配置 Python 环境 |
| 进度显示 | 状态栏显示百分比与已处理采样点数，可随时点击「取消分析」停止 |
| 分析速度 | 采用均匀抽样（约 5000 点）+ 分块并行解码，较旧版提速 3~6 倍，精度经严格验证不受影响 |
| 后台运行 | 分析以低优先级在后台运行，**期间可正常播放、标注、操作软件** |
| 完成   | 折线图显示各 ROI 的亮度曲线，右侧 Y 轴显示音量曲线（如已进行音频分析）                    |
| 自动缓存 | 分析完成后自动在视频同目录生成 `.vla` 缓存文件（VLA2 单文件格式），下次打开时可直接加载   |

分析完成后，折线图的 Y 轴表示平均亮度值（0 ~ 255，0 为黑色，255 为白色），X 轴为时间。如果开启了 Y 轴自动范围（默认开启），图表会自动缩放 Y 轴以适应数据范围。

### 数据导出

| 功能 | 路径 | 格式 |
|------|------|------|
| 导出为 CSV | `导出 → 导出为 CSV` | 亮度数据 `xxx.csv` + 标签数据 `xxx_labels.csv` |
| 保存分析结果 | `文件 → 保存分析结果`（`Ctrl+S`） | `.vla`（含 ROI + 亮度数据 + 音频数据 + 校时 + 标签 + 截图融合 + 辅助线） |
| 加载历史结果 | `文件 → 加载分析结果`（`Ctrl+L`） | `.vla` |

> **注意：** 高版本生成的 `.vla` 文件无法在低版本中打开，反之低版本的文件在高版本中可以正常兼容加载。
>
> **v1.1 存储格式升级：** 新保存的 `.vla` 采用 VLA2 二进制容器——单文件自包含（频谱图内嵌，不再生成 `.vla.spec` 伴随文件）、时间戳/亮度以二进制存储、频谱按显示精度 8 位量化并逐块 zlib 压缩，体积缩小 5~10 倍、加载更快。旧版 JSON `.vla`（含 `.spec` 伴随文件）仍可正常读取。

---

## 六、音频分析

音频分析可以**独立于亮度分析**运行，无需绘制 ROI。

### 启动音频分析

打开视频后，点击工具栏青色 **"音频分析"** 按钮。程序会：
1. 使用 ffmpeg 从视频中提取音轨
2. 计算归一化音量曲线
3. 计算语谱图数据
4. 在折线图右侧 Y 轴显示音量曲线（绿色）
5. 在语谱图面板显示频谱图

| 状态   | 说明                           |
| ---- | ---------------------------- |
| 前置条件 | 已打开视频                        |
| 运行环境 | Release 包已内置 Python 和 ffmpeg |
| 进度显示 | 状态栏显示音频分析阶段和百分比              |
| 完成   | 音量曲线和语谱图同时生成                 |

### 音量曲线

- 显示在折线图右侧 Y 轴，单位为 dB
- 绿色曲线，与亮度曲线共用 X 轴时间
- 缩放/平移折线图时，音量曲线会自动跟随

### 语谱图

语谱图位于折线图下方（如已展开），支持独立的时间轴缩放和双向同步。

| 操作     | 方式                    |
| ------ | --------------------- |
| 时间轴缩放  | 普通滚轮（以鼠标位置为中心）        |
| 语谱Y轴缩放 | `Ctrl + 滚轮`（以鼠标位置为中心） |
| 频率轴平移  | `按住鼠标中键 + 拖拽`         |
| 重置频率范围 | 双击 Y 轴区域              |

**双向同步：** 图表和语谱图的时间轴保持同步。在任一面板缩放/平移，另一个自动跟随。

### 音频降噪

对音频进行频谱门控降噪，改善语谱图和音量图的信噪比。

| 操作     | 方式                       |
| ------ | ------------------------ |
| 调整降噪强度 | 语谱图标题栏的"降噪"滑块（0.0 ~ 2.0） |
| 应用降噪   | 点击"应用"按钮，自动触发音频重新分析      |

**降噪算法：** 基于频谱门控（spectral gating），使用纯 numpy 实现。

**强度说明：** 0.0 = 关闭，1.0 = 标准，2.0 = 强降噪。调整后需重新分析音频才能生效。

---

## 七、A/B 区域循环播放

在图表上设置 A/B 两点，可在两点之间单独播放或循环播放。

> v1.1 起工具栏不再提供 A/B 按钮，请使用快捷键 `A` / `B` 或图表、视频的右键菜单。

| 操作 | 方式 |
|------|------|
| 设置 A 点 | 按 `A` 键，或右键菜单"设置 A 点" |
| 设置 B 点 | 按 `B` 键，或右键菜单"设置 B 点" |
| 切换循环 | 图表或视频右键菜单"开启/关闭循环" |
| 清除 A/B | 右键菜单"清除 A/B 区域" |
| 缩放到区域 | 右键菜单"缩放到 A-B 区域" |

**循环模式：** 到达 B 点自动跳回 A 点继续播放。

**非循环模式：** 到达 B 点自动暂停。

### 导出选段视频（v1.13.0）

> 导出面板为**非模态浮窗**（v1.13.0 起）：打开后播放与游标照常操作，
> 「在当前游标处分段」实时取播放位置；导出进度内嵌面板、可最小化，
> 不挡正常观看。

A/B 选段存在时，工具栏「**导出选段**」可用：把选段合成为单个 MP4（1920×1080）——
上方视频画面、下方亮度曲线与语谱图（各带移动游标，未跑分析的面板自动隐藏）。

- **分段变速**：选段内的标签（`N` 键标记）自动成为分速边界初值；导出对话框中可
  逐段调倍速（0.25x~8x）、在游标处再分段或删除分段点——不关键的段快放掠过，
  关键段常速/慢放。方案随 `.vla` 持久化，重开案件自动恢复。
- **标签入导出**（v1.13.3）：选段内的标签在导出视频的曲线条上打同色竖标+文字；
  播到标签时刻起，画面 OSD 额外烧录一行「🏷 标签内容」（标签色），**显示 5 秒后
  自动隐去**（多标签重叠取最新一个）。
- **图表光标**（v1.13.3）：导出视频的曲线/语谱游标随输出时间动态移动，顶部带
  三角柄（一眼认出是光标不是轴线）；已修正原先按画布全宽计算的右端错位。
- **放大镜入导出**（v1.13.3）：导出时主窗放大镜开着（或多机各瓦片有滚轮放大），
  导出画面右下角嵌入「放大镜 ×N」子窗口（金框），导出时刻的放大取景随帧呈现。
- **完成后一键定位**：导出成功后面板出现「📂 打开所在文件夹」，直接在资源
  管理器中选中产物。
- **信息角标可选**：勾选后烧录「演示副本 · 当前倍速 · 时刻（已校时=北京时间）·
  案件号」，随分段变速动态更新。
- 音频按分段自动 `atrim+atempo` 变速随影；源文件只读不动，产物命名
  `LAClip_<原名>_<A>-<B>.mp4`，案件模式默认存案内 `exports/`。

**自动缩放：** 设置 A/B 后自动缩放图表到 A-B 区域，清除后恢复全时间轴。

**图表同步：** A/B 标记跟随图表缩放/平移自动更新位置。

---

## 八、图表操作与标签

### 图表右键菜单

在折线图区域右键可打开菜单：

| 选项 | 功能 |
|------|------|
| 全部适应 | 恢复 X、Y 轴到全局视图 |
| X 轴适应（全时长） | X 轴显示整个视频时长 |
| 显示/隐藏刻度 | 切换图表底部刻度线 |
| 启用/禁用 Y 轴自动范围 | 切换 Y 轴是否根据数据自动缩放 |
| 添加标签 | 在光标位置添加文字标签 |
| 删除光标处标签 | 删除光标最近的标签 |
| 设置 A 点 / 设置 B 点 | 设置 A/B 循环区域 |
| 清除 A/B 区域 | 取消 A/B 设置 |
| 缩放到 A-B 区域 | 缩放图表到 A-B 范围 |
| 添加水平辅助线 | 按亮度值添加水平参考线 |
| 添加垂直辅助线 | 在光标处添加垂直参考线 |
| 删除此处辅助线 | 删除光标附近的辅助线 |

### 时间轴刻度

图表底部自动显示时间轴刻度尺，包含：

- **起止时间标注**：左右两端显示加粗的起止时间
- **主刻度线**：每个时间标签位置一条粗线
- **次刻度线**：主刻度之间均匀分布 4 条细线，提高可读性
- **时间步长自动选择**：根据可见时间范围自动调整标签密度（1 秒 ~ 6 小时）

### 光标时间显示

橙色色竖线（光标）上方自动显示当前时间，深色背景确保清晰可读。如果设置了时间轴，显示的是绝对时间。

### 用户自定义标签

| 操作   | 方式                                            |
| ---- | --------------------------------------------- |
| 添加标签 | 双击图表区域，输入文字，选择颜色；不选择则随机赋色（6 种预设色：红、蓝、绿、橙、黄、紫） |
| 快捷添加 | 按 `N` 键在当前播放位置添加标签                            |
| 跳转   | 单击标签，视频自动跳转到标签所在的时间位置                         |
| 锚定   | 标签始终锚定在添加时的时间位置，缩放/平移图表后自动跟随                  |
| 删除标签 | 首先选中标签，单击右键-选择删除光标处标签                         |

### 图表缩放与平移

| 操作    | 方式               |
| ----- | ---------------- |
| 缩放时间轴 | 鼠标滚轮（以光标位置为中心缩放） |
| 平移时间轴 | 按住鼠标中键拖拽         |
| 跳转视频  | 点击/拖拽橙色竖线        |

---

## 九、放大镜

放大镜用于查看视频局部细节，停靠在窗口右侧。

| 操作    | 方式                                                     |
| ----- | ------------------------------------------------------ |
| 创建放大镜 | 在视频画面上滚动鼠标滚轮，自动创建                                      |
| 缩放    | 滚轮滚动，以鼠标位置为中心（1.5x ~ 10x）；**视频暂停时同样生效**                  |
| 平移视图  | 按住鼠标中键拖拽（主视频和放大镜内均生效，**暂停时同样生效**），可在主视频窗右键选择‘反向平移’实现反转拖拽方向，以满足使用习惯 |
| 关闭放大镜 | 按 `Esc`，或在视频画面右键选择"关闭放大镜"                              |

**缩放：** 可以通过拖拽主视频视口和放大镜视口间的框线调整视口之间大小，从而获得最佳观看体验。

**放大镜内操作 ROI：** 放大镜锁定后，可以直接在放大镜视图内绘制、移动、缩放 ROI，坐标会自动映射回主视频的全尺寸坐标。

**缩放级别显示：** 放大镜右上角显示当前缩放倍数（如 `2.0x`）。

**来源标识框：** 放大镜开启期间，主视频画面以金色四角括号 + 倍率徽章（如 `2.0×`）实时标出当前放大区域的位置与范围；光标跟随、滚轮缩放、中键平移、画面旋转时同步更新，关闭放大镜即消失。标识框仅为指示，不参与 ROI 框选/拖拽等鼠标交互，且会随证据快照一并烧录（证据自解释：放大图从哪来）。

**截图叠加同步：** 如果已放置截图叠加，放大镜内会自动同步显示裁剪后的叠加效果。

---

## 十、截图叠加

截图叠加用于截取某一帧画面或从外部加载图片，与当前视频进行半透明叠加对比。

### 从外部图片加载

菜单 `文件 → 加载图片为叠加` → 选择图片文件（PNG/JPG/BMP/TIFF）→ 图片出现在截图叠加缩略图中 → 可编辑亮度/对比度/透明度 → 点击放置叠加到视频。

### 工具栏三个按钮

| 按钮 | 功能 |
|------|------|
| **截取** | 截取当前视频帧，截取后左侧出现缩略图窗口 |
| **编辑** | 打开或关闭缩略图窗口 |
| **放置** | 将叠加应用到视频画面（激活时按钮变为橙色背景，再次点击取消叠加） |

三个按钮启用后显示橙色边框，方便辨识当前可操作状态。

### 缩略图窗口

截取帧后，缩略图窗口自动出现在视频画面左侧：

| 状态 | 说明 |
|------|------|
| 缩略图模式 | 小窗口预览截取帧，底部提示"▶ Edit" |
| 展开编辑面板 | 点击缩略图展开，显示大图预览 + 滑块 + 功能按钮 |
| 折叠回缩略图 | 编辑面板中点击预览图 |

### 编辑面板

展开编辑面板后，可以进行以下操作：

| 控件 | 功能 |
|------|------|
| 打勾按钮 | 将叠加应用到视频画面（等同于工具栏"放置"按钮） |
| 垃圾桶按钮 | 清除截图，取消所有叠加 |
| 关闭按钮 | 折叠回缩略图模式 |
| 亮度滑块 | 调整叠加层亮度（-50 ~ +50） |
| 对比度滑块 | 调整叠加层对比度（-50 ~ +50） |
| 透明度滑块 | 调整叠加层透明度（0 = 完全不透明，100 = 完全透明） |

工具栏"放置"按钮和编辑面板内打勾按钮功能一致，互相同步。

---

## 十一、时间戳提取

时间戳提取用于固定视频中的一个区域，将其画面实时显示在一个独立的浮动窗口中。

| 操作 | 方式 |
|------|------|
| 创建 | 按住 `Ctrl+Shift` + 左键在视频上拖拽选择区域 |
| 关闭 | 关闭浮动窗口即可 |

浮动窗口始终显示在最上层，方便在操作其他功能时持续观察某个区域的实时画面。

---

## 十二、视频校时

校时用于把视频流内时间（00:00 起）对应到真实墙钟时间，适用于监控录像等需要对应真实时间的场景。校时一旦采用，图表时间轴、光标时间、标签悬停时间、CSV 导出全部同步生效，并随 `.vla` 分析文件保存。

点击工具栏 **"校时…"** 打开校时窗口（非模态：识别/重建在后台进行，可最小化窗口继续播放/分析；关闭窗口不取消任务，重新打开可见进度与结果）。窗口按使用场景分两组（均为先预填、点"使用"才生效；**v1.12.5 起移除第 3 步高级区**——变速文件 GO 预检会自动判定并进入时间重建，无需手动入口）：

| 分组 | 说明 |
|------|------|
| ① 自动校时（推荐） | 打开窗口时自动做**秒级快速检查**：文件时间正常会提示"使用 ① 即可"；画面时间与播放进度差异大（疑似抽帧/变速）会**自动进入时间重建**（见下「时间重建」节）。点"开始自动识别"：自动在**开头/当前位置/结尾**三处读取画面时间，一句话显示结果（如"识别 3 个取样点，画面时钟每天快 31 秒"）；点"查看细节"可展开取样点表格（截图/原文/可靠度/⚠ 异常标记），可取消勾选异常点自动重新拟合 |
| ② 对真实时间（北京时间） | 监控主机时钟常与真实北京时间有整体偏差（如从未对时）。**v1.12.5 起三种方式**（偏差=北京时间−监控时间，应用到全时间轴/CSV/报告）：**方式一 · 校时图片框选（推荐，取证惯例）**——对监控屏幕拍照时把手机标准时间同框拍入，导入照片后**依次框两个框**（框1=监控主机时间、框2=北京时间，均须含秒；框2只框到时分秒时日期取框1同日，跨日自动取较小偏差并在确认卡注明），OCR 自动识别后弹出**确认卡**（左侧校时图片集成显示、两个框叠加标注，**滚轮放大/拖动平移/双击复位**供逐字校对，右侧两个时间**可直接修改**、偏差行实时重算，原文行只读留证，核对后确认）；校时图片、框坐标、OCR 原文随案件存档（图片复制入 `案件目录/calibration/`，元数据写入 .vla）。**方式二 · 手动输入两个时间**（监控主机时间默认取当前播放位置画面时间，可改）自动算偏差。**方式三 · 直接输入偏移量**「监控主机时间比北京时间 快/慢 X日X时X分X秒」。可填写说明留档，可随时清除 |

其他行为：

- **前处理继承**：用"素材转码拼接"输出的文件随附 `.lumencal.json` 校时信息，打开时自动继承为**分段校时**——每段时间轴按其画面 OCR 时间锚定（感知重叠修剪/丢弃/转码失败，转码段按产物实测时长锚定）；段间时间缺口处墙钟精确跳变（监控常态），打开时弹出缺口提示（该提示也会写入报告）。
  - **缺口在时间轴上的显示**（v1.12.3）：时间轴保持播放连续（流内坐标），刻度只标注**真实有素材**位置的墙钟；每个缺口处画**红色虚线竖线**并标注红色小字「缺 10.3h」——一眼可见"这里缺了一段"。点击缺口位置 seek 自动**跳到缺口后第一段**（跳过没录的）。**v1.12.4**：缺口红字与时间轴刻度重叠时自动**只留红虚线**（刻度优先），放大到不重叠时红字自动恢复显示。
- **不应用漂移修正**：①的识别结果若含漂移修正，可勾选此项退回固定偏移。
- **未校时**的视频行为与旧版一致（图表显示流内时间）。

例如：三点识别拟合出"流内 0 点 = 2026-07-22 08:15:00，漂移 +40 秒/天"，则图表时间轴、光标、CSV 导出均按此换算；再用②输入对时结果，可再叠加与真实北京时间的整体偏移。

### 时间重建（变速/抽帧文件）

部分 DVR/NVR 采用**抽帧录像**策略：无异动时按 12.5/16/20fps 等低帧率录像，有异动时切回 25fps 全帧率；导出时统一按 25fps 重打时间戳，导致"流内时间"被压缩（例如 74 分钟流内对应真实 103 分钟），画面 OSD 显示的时间"走得比播放快"。此类文件无法用 ① 的单条直线模型校时（拟合速率会超出合理上限而被拒绝）。

**打开校时窗口时的快速检查**会自动发现这类文件并**自动进入时间重建**（v1.12.5 起无需手动入口）：

1. **粗采样**：全片约每 60 秒取一帧 OCR（约 1~2 分钟）；
2. **分段检测**：自动找出斜率跳变位置（变速边界），剔除 OCR 错读的异常测点（⚠）；
3. **边界加密**：在边界附近密集取样，把每个边界定位到 ±2 秒内；
4. **音频校验**：用音轨真实时长核对 OSD 总跨度，不一致会明确警告。

完成后展示**分段表**（每段流内范围/斜率/OSD 起点），点"采用重建结果"生效：

| 段 | 流内范围 | 斜率 | OSD 起点 | 说明 |
|----|----------|------|----------|------|
| 1 | 00:00 – 08:20 | 1.000 | 2026-07-22 14:59:43 | 按画面 OSD 重建 |
| 2 | 08:20 – 16:40 | 2.000 | 2026-07-22 15:29:15 | 按画面 OSD 重建 |

- 斜率 1.0 = 正常录像；斜率 2.0 = 画面钟以 2 倍速走动（12.5fps 抽帧导出）；
- 正常录像（无变速边界）会自动判定并提示，行为与 ① 一致，无需等待；
- 测点表中标 ⚠ 的为 **OCR 异常测点**（读数疑似错读，已自动排除），引用其时间时须以证据帧截图为准；
- 重建结果（分段表/测点/异常标记）随 `.vla` 保存，报告中可追溯。

---

## 十三、素材转码拼接（前处理）

把多台监控摄像机导出的几十段零散录像，整理成**顺序可证明**的连续时间线，并拼接成一个文件。**支持 DAV（大华）、AVI、WMV、FLV、TS/MTS/M2TS、MOV、MKV、MPG/MPEG、3GP、WebM 等格式**——非 MP4 自动统一转码为 MP4（H.264，2 秒关键帧），拖拽播放顺滑连续；参数一致的 MP4 直接无损拼接。

### 什么时候用

- 一台 DVR/NVR 按时间分段的录像（每段几分钟到几十分钟），需要按真实拍摄顺序拼成一段完整视频；
- 文件名被重命名过、metadata 丢失，只能靠**画面里烧录的时间戳**确定顺序；
- 素材是 AVI/WMV/FLV/TS 等非 MP4 格式，需要统一格式后才能顺畅分析。

### 打开方式

主窗口工具栏右侧的 **「素材转码拼接」** 金色按钮（或菜单 文件 → 素材转码拼接，快捷键 Ctrl+M）。弹出独立任务窗口，顶部为**功能说明**（用法 + GO 自动判断矩阵 + 输出说明）。窗口与分析界面互不干扰，可并行使用。

### ① 导入素材（一个 GO 完成）

把视频文件**直接拖进窗口**（或点「添加文件」）。列表逐行显示时长/分辨率/格式/大小。**列表行可以直接上下拖拽调整顺序**（当前顺序即拼接顺序）。

- **自动排除重复文件**：拖入时按内容去重——尺寸不同的文件必然内容不同（免哈希）；仅**同尺寸**的文件计算 SHA-256 指纹比对，指纹一致判为重复并**保留首个**，其余不入列（状态栏明示排除清单）。同一批监控素材常混入重复导出的片段，此规则防止拼接产物出现重复画面。

- **「GO」（右下角大按钮，推荐）**：一键完成——**读取每段首尾帧的画面时间 → 自动排序 → 拼接出结果**。识别中实时显示进度；同一台录像机的一批素材，认出第一段后其余段自动套用同位置识别（快且准）。
  - 全部认出 → **直接拼接出结果**，无需任何多余操作；片段间时间重叠**自动修剪**（剪后段开头、保前段完整，报告逐段留痕），不再打断流程；
  - 个别段没认出时间 → 软件按前后段**推算它的位置**（标「估算」，报告留痕），段数少时直接拼；
  - 拿不准的才停下来给你看（见②）；
  - 拼接方式自动判断：参数一致的 MP4（关键帧 ≤2 秒）**无损直拼**（不重编码、画质零损失）；含非 MP4 或关键帧稀疏 → **自动转码后拼接**（统一 MP4·H.264·2 秒关键帧）；只拖 1 个文件 → 单独转码导出。
- **「直接拼接（不识别时间）」（小字旁路）**：不识别画面时间，按列表当前顺序直接拼接（可拖行定序）。顺序已知时用，最快。**护栏**：文件名全部不含时间信息且尚未识别排序时，会先弹乱序风险提示（拖入顺序由拖拽锚点决定，监控素材极易乱序，且重叠重复片段不修剪），可一键改走 GO。

### ② 确认顺序（只在拿不准时出现）

顺序可信时本页自动跳过。有拿不准的段时，只显示**问题卡**（其余已就绪段折叠）：

- **没认出时间的段**：卡片大字「『xx.mp4』没认出画面上的时间」+ 首帧截图。点「**框一下画面上的时间**」在截图上拖个框 → 该段与同批其余段自动跟着认（同一台录像机只需框一次）；也可「手动输入…」或「按当前位置拼接（知道就好）」。
- **时间重叠的两段**：显示两张截图，点「**交换前后**」一键换位，或「保持现状」。
- **整组没认出时间**：一张组级卡——「框一段画面上的时间，其余段会自动跟着认」，或确认按文件信息顺序拼接。
- 顶部提示「还剩 n 处待确认」，全部处理后主按钮变为「**✓ 确认顺序，继续拼接**」。
- 点「**查看全部片段**」可展开完整时间线与证据卡（高级核对）：每段色块位置=拍摄时间、宽度=时长，缺口灰斜纹、重叠红叠加；双击卡片看大图，拖卡调序，悬停看 OCR 原文（逐字）。

> 位置为推算的段在证据报告「衔接警告」列与完成页醒目提示中均有留痕。

### ③ 拼接设置

程序已根据片段参数自动判定拼接方式：

- **无损拼接（推荐）**：各段参数一致时直接拼接，不重编码、画质零损失；
- **统一格式后拼接**：部分文件需要转码（原因会明示，如“有 2 个文件格式不兼容”）——**只转确实需要的文件**，其余仍无损拼接。

**v1.7.0 新增（前处理 v2）**：
- **硬件编码可选**：高级选项新增「编码器」下拉——默认 libx264 软件编码（与旧版一致），NVIDIA 显卡可选 NVENC、Intel 核显可选 QSV（需 GPU 支持，自动探测可用性）。转码产物的处理动作在证据报告中记录实际编码器（取证留档）。
- **时间重叠自动修剪**：同组多机同时段录制、预检检测到重叠时**默认自动修剪**——剪掉后一段的重叠开头、保留前段完整（消除时间线重复），运行日志与证据报告逐段留痕；如需保留原样，可在拼接设置「高级选项」勾选"保留时间重叠段原样"。
- **并行转码**：多组素材转码时组间并行（同组仍顺序），长任务提速明显。
- **自动命名**：转码产物自动命名为 `<通道>_<日期>_<起止时间>.mp4`（如 `CAM01_20260722_172528-173541.mp4`，基于校时墙钟）；无校时信息时回退原命名规则。
- **跨相机混拼归一**：同组分辨率不一致时统一到主导分辨率再拼接（保持宽高比）。

技术细节（各段参数对比）默认折叠，可展开。选择输出文件夹（默认在素材目录下新建 `LumenArc_Merged_<时间戳>/`），页面会显示**预计输出大小与磁盘余量**。高级选项（时间戳归一化/CRF 画质/反交错/哈希）默认折叠。点「开始拼接」。

### ④ 执行与报告

大进度条 + 当前处理段 + 预计剩余时间；详细日志默认折叠。完成后显示结果卡片：

- **证据摘要**（默认展开，可直接选中复制）：一行结论（导入 N → 排除重复 X → 识别/推算/人工 → 输出 M 个文件）＋**覆盖时间范围**（最早段首 → 最晚段尾）＋**缺口逐条明细**（每条给出缺了的墙钟时间段与时长，如 `16:33:17 → 次日 02:53:54（缺 10.3 小时）`）＋重叠修剪/整段丢弃留痕＋排除重复清单。数字与产物随附的分段校时（.lumencal.json）同口径；
- **「在主窗口播放输出」** 一键把拼接结果载入主窗口播放校验（无需手动拖入）；
- **「打开输出文件夹」** 直达输出视频（输出文件名为 `merged_concat.mp4`，多组时按组名命名，重名自动加序号）；
- **「查看证据报告」** 打开 `report.csv`（每段的 OCR 原文、解析时间、文件名、哈希、处理动作）；
- 证据目录 `LumenArc_Evidence_<时间戳>/` 内含首/尾帧截图、拼接清单、操作日志。

失败时给出平实原因与建议动作（如"磁盘空间不足，更换输出文件夹后重试"），日志全部保留。

> **取证原则**：相机时钟偏差不准确时，报告如实呈现原始观测值与偏差说明，程序**绝不静默修正**原始数据。排序结论 = 画面截图 + OCR 原文 + 文件名原文 + metadata 原文 + 人工确认记录。识别失败的片段一律排在末尾标 "?"，由人工看图输入时间兜底——证据链不依赖 OCR 精度。

### 关于 Dahua (.dav) 素材

程序直接支持大华 DVR 导出的 `.dav` 文件：其内部写有**录制时刻**（不依赖画面识别），
排序依据显示为「✓ 流内录制时间」；画面时间识别作为交叉校验。由于 .dav 是私有格式，
拼接前会自动统一转换为 MP4（H.264，画面质量预设 CRF 18）。转换后的时间与原始
录制时间逐段对应，证据报告中同时留档流内起始墙钟（派生列）。

### 注意事项

- 无损拼接要求片段参数一致（同编码/分辨率/像素格式）；不一致时程序自动先统一格式。
- 时间重叠片段默认自动修剪（剪后段开头、保前段完整，报告逐段留痕）；高级选项可改为保留原样拼接（仅报告标注）。
- 窗口在执行任务时关闭会询问是否取消任务；已完成的证据不受影响。

---

## 十四、案件管理（v1.3）

把一起火灾案件的全部视频、校时、分析结果、前处理成果**归拢到一个案件文件夹**统一管理：分析数据自动存入案件目录、源视频登记指纹、换机/移交一键打包。不使用案件时，软件行为与旧版本完全一致（**独立模式**）。

### 什么时候用

- 一个案件涉及多路监控、多次校时与多份分析，需要**一处归档、可校验、可移交**；
- 需要把案件材料**完整移交给另一台电脑**（移交包含视频副本，开箱即用）；
- 临时看一两个视频 → 不需要建案件，直接 Ctrl+O 打开即可（独立模式，行为同旧版）。

### 新建与打开

- **菜单 案件 → 新建案件**：填写名称/调查员/单位/案发日期/城市/区县（必填），**编号自动生成**（`YYYYMMDD-城市区县-x`），创建后固定；案件目录 = `编号-名称`，位于案件根目录下（菜单 案件 → 案件根目录设置 可改，默认 `<程序目录>/cases/`）。
- **起始页**：启动时可选新建/打开/最近案件，或直接进入独立模式（可勾选不再显示）。
- **打开案件**：选择案件目录；若提示锁占用（其他实例正在用或上次未正常关闭），可强制打开。案件菜单的**最近案件**子菜单记录最近 10 个。

### 案件面板（证据树）

打开案件后，左侧出现**案件面板**替代视频列表，分四组：视频 / 前处理会话 / 报告 / 快照。每路视频带徽标：

| 徽标 | 含义 |
|---|---|
| ⏳ | 指纹待计算（后台队列自动补算） |
| ✓ | 指纹一致（源文件与登记一致） |
| ⚠ | 源文件已变更（大小/时间不符） |
| ✗ | 源文件缺失（右键可重新定位） |
| ⏰ | 已完成校时 |
| 📦 | 使用移交包内副本（原路径缺失时自动兜底） |

右键视频：打开播放 / 重新定位 / 批量重新定位 / 移除出案件（默认保留案内数据）/ 计算指纹 / 复制指纹 / 在资源管理器中显示。**双击**打开播放。

### 机位组（v1.15.0，组织轴心；v1.15.3 机位独立编号）

> 案件里的一切检材都归属**机位组**：每个机位有**自动排序的机位编号**
> （C01/C02…，创建时分配、永不复用、改名不动），另可加**助记中文名**
> （如「烟酒店」，随时改）：
> **直接导入的视频**入案时自成新组；**前处理产物**登记完毕时逐产物询问
> 「归入已有摄像头 / 创建新摄像头」（通道名匹配既有组会自动预选）。
> **只有同机位组的文件才能同轴播放**（多机时间线的合并轨按组归并）。

- **案件树按组分层**：📷 组节点 → 成员文件（原件 V### 与产物 P### 同待遇
  混排，指纹/校时徽标照常）。
- **改名**：组节点右键「机位组改名」——对全组生效，时间线/点位图/报告
  全系统跟随（改名不动内部键，引用零牵连）。
- **归组调整**：文件行右键「移到机位组 ▸」（现有组或新建组）。
- 老案件打开时自动迁移：同摄像头编号的文件聚为一组，未编号的各自成组。

### 视频入案

- 案件打开时，**Ctrl+O 或拖入视频即自动入案**（登记大小/时间、自动排队算指纹）；源视频旁若有已保存的 `.vla` 分析结果，会询问是否复制导入案件（默认是）。
- 菜单 文件 → **临时打开视频（不入案）**：只想看一眼的文件，与独立模式行为一致。
- 入案后：`.vla` 分析结果自动改存 `<案件>/videos/V###.vla`；校时证据帧归入 `<案件>/evidence/`；时间戳框选记忆随案保存（下次开案自动恢复）。

### 前处理成果导入案件

案件打开时，素材转码拼接窗口顶部出现**案件横幅**：默认【导入案件】——拼接输出、报告、证据截图自动登记进案件（`<案件>/preprocess/<时间戳>/`）；选【独立输出】则与旧版行为一致。

### 指纹与完整性校验

- **自动后台指纹**：视频入案即排队计算 SHA-256（单线程低优先级，不影响 4K 播放）；源文件变更后自动重算。
- **完整性校验**（右键单路或全量）：比对登记指纹，报告每路「一致 / 已变更 / 缺失」。快扫（大小/时间）为尽力而为，**确证篡改请以「全部重算」为准**。

### 重新定位（源文件被移动/删除后）

- **单路**：右键 → 重新定位，选新路径（只改引用，不动案件数据）；大小不一致默认拒绝，可显式「仍要采用」（轨迹留档）。
- **批量**：菜单 案件 → 批量重新定位（或右键）——选候选目录自动按**文件名+大小**模糊匹配，人工确认后**强制指纹比对**：一致即采用，不一致默认拒绝、可显式【仍要采用（留档）】。

### 导出移交包

菜单 案件 → 导出移交包：

- **完整包（默认）**：案件目录 + `sources/` 视频副本——**换机零操作可用**（原路径缺失时自动使用包内副本）；
- **轻量包**：不含视频副本，接收方需自行准备源视频并重新定位；
- 导出前自检：缺失指纹可即补、缺失源可即定位、磁盘空间预检（不足阻断）；包内含 `README.txt`（案件信息+指纹清单）与 `manifest.json`（完整性清单），导出后自动快速校验副本一致性。

### 生成分析报告（v1.14.0）

> 案件菜单「生成分析报告(&G)」→ **自检与补录对话框** → 生成 **DOCX 草稿**
> 到案内 `reports/火灾视频分析报告_时间戳.docx`。
>
> **① 生成前自检**：❌ 阻断项（源文件缺失/全部未校时）须先处理；⚠️ 提示项
> （审核/批准人未填、点位图未绘、无标签节点等）可放行，报告相应位置留白。
> **② 信息补录**：审核人/批准人/送检人 + 逐路拍摄方向/提取方式/存储介质，
> 保存入案件（下次生成自动记忆）。

- **自动填入**：封面（案件编号/火灾名称/单位/分析人/日期）、静态目录、
  基本情况（案情=案件备注）、检材清单表与逐视频物理属性（格式/大小/
  分辨率/帧率/编码/时长/**MD5+SHA-256 双哈希**，生成时自动补算）、
  时间校准结果表（显示时间/校时方式/时间差/校准公式）、校准证据截图、
  **接力对时取证链**（逐跳事件名+容差+累积容差声明）、火势发展时间表与
  关键节点骨架（来自图表标签，北京时间口径）、分析意见（起火时间由最早
  标签预填「不晚于 …」）、局限性说明（OSD 疑似错读点自动列出）、附件
  （快照关键帧嵌入 + 导出片段清单）、落款签名盖章留白位。
- **拼接记录列为证据**（v1.14.0）：前处理拼接产物在报告「二（四）」节列出
  产物↔来源文件全表（来自案内 preprocess/ 证据目录的 report.csv），附
  operations.log 关键决策行与原件路径引用；附件清单同步登记。
- **留白待手填**（DOCX 可编辑）：画面描述/分析结论/起火部位/人物车辆活动等
  人工撰写位。
- 监控点位图：案件菜单「编辑监控点位图(&M)」绘制出图后重新生成报告自动嵌入。
- 五（三）节自动嵌入逐路**亮度变化曲线图**（横轴北京时间；需已做亮度分析）。
- 大文件哈希补算在点「生成报告」后带**进度条**进行（可取消即弃稿）；
  自检/补录阶段不算哈希、秒开。

### 监控点位图编辑器（v1.14.0，P-74）

> 案件菜单「编辑监控点位图(&M)」——导入现场平面图/地图截图，布监控点位，
> 一键生成**带标准图框**的成品示意图（2480×1754 PNG 存案内
> `reports/assets/sitemap.png`），报告「二（三）监控点位图」自动嵌入。

- **导入底图**：选图后弹**裁切对话框**——拖框保留有效区域（或整张使用），
  确认后图片复制入案并**固定适配画布**（不再平移缩放，防误挪）。
- **布点**：左侧机位**按物理机位分组**（同一监控的原件与拼接产物
  V###/P### 聚为一组，一组一行），拖到画布落点；同机位再拖=挪位；
  组名未自定义时附源文件名提示，悬停 tooltip 看成员全清单。
- **机位改名**：选中组后点「机位改名」——改的是**机位组名**（全系统
  跟随，点位引用稳定组编号零牵连）。机位组也可在案件树里直接管理
  （改名/移到组/新建组），两处等价。
- **调方向/大小**：点选点位 → **滚轮转朝向、Shift+滚轮调张角、
  Alt+滚轮调标注字号**（50%~300%；画布底部有显眼横幅提示），或属性条
  精确输入朝向/张角/半径/字号；扇形=拍摄方向与覆盖范围，点位标注字色
  与扇形同色（无框，机位色与多机时间线一致）。
- **图上标注 = 机位编号**（C01/C02…；助记名只在左侧栏/案件树看，
  图面保持最短）。文件编号（V01/P01 两位）是文件身份，机位编号（C##）
  是摄像头身份，两套独立。
- **出图保存**：双图框+右下角标题栏（案件编号/图名/制图/审核/日期/图号）。
- 机位从案件删除后，对应点位标红「已移除」不自动删（人工决定去留）；
  机位改名图上标签自动跟随。
- 一案一张，坐标归一化存 `sitemap.json`（换底图不失位）。

### 多机同步播放

菜单 案件 → 多机同步播放（案件开着即可进；独立大窗口，v1.10）；无案件时亦可从 文件 → 多机对比播放（2 路） 进入（双临时进槽位）。

**第一步：勾选机位**。开窗先给清单——案内全部视频与前处理产物（拼接/转码输出）逐路列出，校时状态实时读取案内分析文件标识：✅ 已校时 / ⚠ 未校时 / ❌ 文件缺失（不可选）。已校时路默认勾选（≤4），按需增删后点「**开始同步播放**」：

- 勾选 2~4 路且全部已校时 → **合并时间线模式**；
- **同编号多文件可并成一路（v1.13.3 合并轨）**：清单顶部自动列出「⊞ 机位标签 · N 段合并轨」组行（同机位编号 ≥2 个文件且全部已校时才出现；任一文件未校时则整组不出现——先补齐校时）。勾选组行即把该机位的多个文件并成**一路**播放：引擎自动跨段换文件，时间线该行内按段多色块显示，段与段重叠时「先起步的段赢」，被压住的重叠区画斜纹并标 ⚠；瓦片名称带段标 [k/N]。合并轨与单文件路可混选（组合计 1 路，总计仍 ≤4 路）。勾选组行后其成员行自动禁选，反之亦然。
  注意：合并轨会话暂不支持「导出选段」（请把各段分别作单路加入再导出）。
- 勾选 2 路且含未校时 → **分开进度条模式**（临时对齐）；
- 勾选 3 路以上含未校时 → 开始钮禁用并提示（多路对齐精度靠校时保真：点该行「**去校时…**」到主窗完成时间设置，回来点「**刷新清单**」；或减到 2 路走临时对齐）；
- 播放中可点工具行「**↩ 重选机位**」返回清单换路。

**第二步：同步播放**。

- **北京时间偏移全程生效**（v1.12.7）：各路做过「对真实时间（北京时间）」后，多机合并时间线/瓦片 OSD/游标报时**一律按北京时间口径**（未做对时的路仍按监控画面 OSD 时间）；
- **画面调节**（v1.12.7 起；v1.12.8 起**逐机位独立**）：工具行「画面调节」钮调出悬浮面板——亮度/对比度/伽马/色阶/反色 + 90° 旋转，**只作用于当前选中的瓦片**（点击瓦片切换作用对象，选中者有强调色外框；面板标题显示当前机位，切换时自动回填该路已调参数；每路独立记忆）（仅显示链路，引擎原始帧与证据链不变；与单路分析同一套参数公式）；
- **选段导出**（v1.13.0）：模式 A 合并时间线上按 `A`/`B` 键在游标处打点（`Ctrl+A`
  清除），工具行「导出选段」→ 与单路同款分段变速对话框（墙钟域显示），合成
  **全机位宫格 + 各路覆盖条 + 移动游标**的 1080p MP4；某时刻无机位画面的格
  自动显示「该时刻无画面」占位；音轨取自当前主听机位（未全程覆盖选段时产物无
  音轨并在完成提示注明）；
- **合并时间线模式**（≥2 路全部已校时）：底部条图每路机位一行，色块=该机位**墙钟覆盖**（按真实时长，由各自校时换算）——红色叠加=多机同时覆盖、灰色斜纹=缺口；**拖动游标**全路实时追逐同步走带；
- **分开进度条模式**（2 路且含临时进路）：每路各自带时间的进度条，纯画面复勘（无亮度/音量/语谱面板）；**对齐前两路各自独立**——拖进度条只动本路、时间口径为各自流内时间（另一路不受影响、也不会显示「无信号」）；**确认对齐后两路联动**——拖任一路进度条同步走带，临时路时间行附「≈墙钟」推算参考；
- **临时进**：2 路界面允许未校时视频手动对一个时间点（画面或声音事件）临时加入同步——**会话级，不落盘、不写案**，关窗即失效，瓦片以「临时对齐」角标明示；对齐时点「对齐…」分别拖两路到同一时刻（点瓦片可切换听音，以声定点）再「确认对齐」；
- 单击瓦片=切换发声路（🔊）；**悬停滚轮=该瓦片独立放大镜**（1x~8x）、**中键拖拽=平移**；**双击瓦片/色块**回主窗单路分析；OSD 角标可一键开关；
- 多路高负载时自动性能治理：优先硬解，超重路自动降「预览降清档」（仅预览降清，不碰原始数据），状态栏提示不强制；
- 标题栏带原生**最小化/最大化/关闭**按钮（与素材转码拼接页同款，多路铺屏看细节一键铺满/还原）。

> 对齐精度取决于各路校时精度；临时进路为手动对齐，精度更低，角标单独明示。

#### 同事件对时（给没对过表的机位修钟，v1.13.3 重做引导）

**一句话原理**：某路视频没校过时间？在两路画面里找到「同一件事的同一
瞬间」告诉软件，它就能算出这路的钟差多少——找到 1 次能修**整体偏差**；
找到 2 次还能修**越走越快/慢**（第 2 次自愿，不强制）。

多机同步播放页工具行「同事件对时」进入。右侧面板分 ④ 段，跟着顶部
横幅走就行（横幅永远显示「现在该干嘛」）：

1. **① 谁的钟是准的？** 选已校时机位（经别的机位接力对过的会注明）；
2. **② 要修谁的钟？** 选没校时的机位；
3. **③ 打「同一瞬间」标记**：找一件两路都看得见的事（同一辆车过门口、
   同一声喇叭、有人挥手、灯亮灯灭），用「微调画面」逐帧把两边都对到
   那个瞬间，**给这件事起个名**（必填，报告里要念出来），点
   「📍 就是这一瞬！打标记」；
4. **④ 预览与保存**：每记下一个标记，列表立刻用大白话显示结论
   （如「目标的钟慢 2 分 14 秒」）。点「▶ 应用预览」播放试看——两路
   贴齐了点「💾 保存校时」。预览后每个标记附**对时误差**，超过半秒
   会红字提醒「这对标记可能找错事了」。

保存时弹**取证链确认卡**：逐级列出从绝对校时锚到本路的完整传递链
（每跳事件名/两路时刻/容差）+ 累积容差声明，确认后写入案件留档。

规矩：多跳接力允许（C 可参考接力对过的 B），**成环禁止**（链成环无法
陈述，打标记时自动拒收）；间接对时可信度天然低一档（最高 0.8），
瓦片/确认卡如实标注「间接·N 跳·累积容差 ±Xms」。取证链随校时一体
存入 .vla。

### 关闭案件与模式出口

三处出口等价：案件面板 ✕ / Ctrl+W / 状态栏 📁 按钮。有未保存变更时提示保存；**关闭不中断当前播放**，重开案件时自动恢复上次查看的视频与分析现场。

> **取证红线**：源视频永远只读，重定位只改引用路径；「仍要采用」等覆写操作全部留档于 case.json；案内分析数据以 `.vla` 为唯一真源。

## 附录

### 键盘快捷键一览

| 按键 | 功能 |
|------|------|
| `空格` / `k` | 播放 / 暂停 |
| `←` / `→` | 后退一帧 / 前进一帧 |
| `↑` / `↓` | 音量 +5 / -5 |
| `c` / `l` | 加速一档 |
| `x` / `j` | 减速一档 |
| `z` | 恢复 1x 倍速 |
| `n` | 在当前播放位置添加标签 |
| `A` | 设置 A 点 |
| `B` | 设置 B 点 |
| `P` | 进入多边形模式 |
| `G` | 进入辅助线模式 |
| `Esc` | 关闭放大镜 / 退出当前模式 |
| `Delete` | 删除选中的 ROI / 辅助线 |
| `右键` | 删除鼠标下的 ROI / 辅助线（无需先选中） |
| `Ctrl+O` | 打开视频文件 |
| `Ctrl+W` | 关闭案件（仅案件模式） |
| `Ctrl+S` | 保存分析结果 |
| `Ctrl+L` | 加载分析结果 |
| `Ctrl+Shift+C` | 复制 ROI |
| `Ctrl+Shift+V` | 粘贴 ROI |

> 在程序中点击 **帮助 → 快捷键速查** 可查看完整快捷键列表（半透明弹窗）。

### 常见问题

| 问题 | 解决方法 |
|------|---------|
| 分析失败 | Release 包自检通过即无此错误（v1.8.0 分析引擎进程内运行，不依赖外部 Python）；OCR 校时失败才与 Python 相关：内置环境无需安装，源码版需配置 |
| 分析失败 "ffmpeg not found" | Release 包已内置 ffmpeg；本地源码开发版需确保 ffmpeg 在 PATH 中或在 `portable/ffmpeg` 目录 |
| 打开视频黑屏 | 确认视频格式支持，不支持的格式先转为 MP4 |
| DAV 格式无法打开 | DAV 是大华私有格式，请先转为 MP4 |
| 截图叠加不显示 | 需先点"截取"，再点"放置"才叠加到视频 |
| 放大镜内 ROI 不生效 | 直接在放大镜视图内操作 ROI |
| VLA 文件打不开 | 高版本生成的 `.vla` 无法在低版本中打开；v1.1 可正常读取所有旧版 `.vla`（含旧 `.spec` 伴随文件） |
| 音频分析后没有音量曲线 | 确认点击的是"音频分析"按钮，且视频包含音轨；无需绘制 ROI |
| 亮度分析按钮灰色 | 确认已打开视频并绘制了至少一个 ROI |
