# 追光者 Lumen Arc — 操作手册

火灾调查视频分析软件 · 本手册随安装包发布，始终对应当前软件版本（版本号见启动画面与「帮助 → 关于」）

---
本项目由广东省火调技术中心发布并维护，采用 Apache 许可证 2.0 版进行授权。
---

## 目录

- **一、认识软件** — 功能、运行环境、界面、两种使用方式
- **二、十分钟快速上手** — 两条典型动线
- **三、案件与前处理** — 建案、机位组、素材拼接、指纹与移交
- **四、校时体系** — 自动校时、对北京时间、时间重建、缺口显示
- **五、单路分析** — 播放、画面调节、副屏全屏、ROI、亮度、音频、图表、放大镜、截图叠加
- **六、多机同步播放** — 合并时间线、合并轨、同事件对时
- **七、产出与移交** — 证据快照、导出选段、点位图、分析报告、移交包
- **八、附录** — 快捷键、常见问题、相关文档

---

## 一、认识软件

**追光者 Lumen Arc** 是一款面向火灾调查的视频分析桌面工具：对画面指定区域逐帧计算平均亮度并绘制曲线，同时支持独立音频分析（音量曲线 + 语谱图）、多机同步播放、视频校时、证据快照、分析报告生成，帮助你从视频和音频两个维度提取并固定关键证据。

### 运行环境

| | 最低配置 | 推荐配置 |
|---|---|---|
| 操作系统 | Windows 10 1809+ 64位 | Windows 10 22H2 / Windows 11 64位 |
| CPU | 4 核（i5-6500 / Ryzen 3 1200 级） | 6 核 12 线程以上 |
| 显卡 | 任意支持 D3D11 的核显 | Intel UHD 620+ / 任意近代核显或独显 |
| 内存 | 8 GB | 16 GB |
| 存储 | 机械硬盘可读，素材建议放 SSD | NVMe / SATA SSD |
| 显示器 | 1920×1080 | 2560×1440 或以上 |

- 本软件**不吃显卡**：视频解码走核显 D3D11VA，画面绘制为 CPU 路径，预算优先给 CPU 和 SSD。
- 性能瓶颈实测排序：素材在机械盘的冷 seek > CPU 软解吞吐 > 内存。长视频快速拖拽对随机读取敏感，放 SSD 上手感差异明显。

### 主要功能一览

| 功能 | 说明 |
|------|------|
| 亮度量化分析 | 框选矩形或多边形 ROI，逐帧计算区域平均亮度并绘制曲线 |
| 音频分析 | 无需 ROI，直接提取音轨生成音量曲线和语谱图，支持降噪 |
| 视频校时 | 画面时间自动识别（含漂移修正）、对北京时间、变速文件时间重建 |
| 多机同步播放 | 多路按墙钟对齐同屏播放，合并时间线，同事件对时 |
| 前处理拼接 | 零散监控片段按画面时间自动排序、拼接，证据报告留痕 |
| 案件管理 | 视频/校时/分析/报告一处归档，指纹校验，一键移交 |
| 放大镜 | 局部放大查看细节，可在放大镜内绘制 ROI |
| 证据快照 | 一键合成分析现场全图（画面+曲线+语谱） |
| 导出选段 | A/B 区间合成带曲线游标的 MP4，支持分段变速 |
| 分析报告 | 一键生成 DOCX 报告草稿，监控点位图编辑器配套 |

### 账号登录

首次启动（或登录态失效）时，软件会在主界面上弹出登录窗口，两种登录方式任选：

- **手机号登录（推荐）**：输入手机号 → 点「发送验证码」→ 输入短信验证码 → 登录。
  系统会自动判断该手机号是否注册过：
  - 已注册 → 直接进入，无需再填任何信息；
  - 未注册 → 自动展开注册补全区，填写**姓名和单位**后完成注册。
    注意：您填写的姓名和单位就是您的**署名**，会直接写进案件录入、分析报告
    和监控点位图的落款处且不可在各处单独修改；如需变更，请走
    「帮助 → 账号管理」（需当前手机号短信验证），变更对之后新建的案件生效。
- **邀请码激活（无网络环境的机器）**：切换到「邀请码激活」页签，输入提供方发放的
  邀请码 + 本人姓名 + 单位 → 「激活并登录」。每个邀请码只能激活一次；**激活瞬间
  需要联网一次**，激活成功后长期离线可用。

登录一次后，联网正常使用即可长期保持登录态；长期断网超过一个月会被要求重新验证一次
（联网收个验证码即可，数据不受影响）。

登录后可通过「帮助 → 意见反馈」随时向开发者提交问题或建议，可勾选附带诊断信息
（仅软件版本、系统信息、上次异常退出记录，不含任何案件数据）。

### 启动与打开视频

双击 `LumenArc.exe` 启动。打开视频的方式：

| 方式 | 操作 |
|------|------|
| 菜单打开 | `文件 → 打开视频`，或按 `Ctrl+O` |
| 拖放打开 | 直接把视频文件拖入程序窗口 |
| 加载历史分析 | `文件 → 加载分析结果`（`Ctrl+L`）——`.vla` 分析文件可跨设备传递 |
| 自动加载缓存 | 打开视频时若同目录有已保存的 `.vla`，会提示是否直接加载 |

支持格式：MP4、AVI、MKV、MOV、WMV、FLV、WEBM 等常见格式（DAV 大华格式请先在「素材转码拼接」中处理）。

> **播放内核**：自研 FFmpeg 内核，对监控导出的"伪 MP4"（实际为 MPEG-PS/TS 流）提供精确到单帧的拖拽和逐帧步进；支持 D3D11VA 硬解（`设置` 菜单可开关、指定适配器，重启生效），拖拽时自动切换多线程软解追赶保证落点精确。倍速播放时音频变速；拖拽进度时播放与光标对齐的短音频片段。

### 界面布局

- **左侧**：视频列表面板（独立模式）或案件面板（案件模式），可折叠
- **中间**：视频播放区 + 折线图/语谱图面板
- **右侧**：放大镜面板（视频上滚动滚轮自动出现）——左半原始画面、右半放大镜等大同高并列，分割条可拖；左右侧栏在放大镜打开时自动收成细条、关闭自动还原
- **顶部**：工具栏（播放控制、分析、ROI 模式、校时、快照等）
- **底部**：状态栏（分析进度与操作反馈）

「画面调节」面板为独立浮窗呼出，不挤占布局，可拖到任意位置。

### 两种使用方式

| 方式 | 适用 | 行为 |
|------|------|------|
| **独立模式** | 临时看一两个视频 | 直接打开即用，分析结果存视频同目录 `.vla` |
| **案件模式** | 正式办案 | 全部检材/分析/校时/报告归拢案件目录，可校验可移交（见第三章） |

### 中英双语

菜单 `帮助 → 语言` 切换中文 / English，切换后自动重启。

---

## 二、十分钟快速上手

### 场景 A：临时分析一路视频（5 分钟）

1. `Ctrl+O` 打开视频；
2. 工具栏点「**矩形**」，在视频上拖出关注区域（如门口、窗户）；
3. 点「**亮度分析**」，等进度走完——折线图出现亮度曲线，拖曲线上的橙色竖线跳转视频；
4. 播放到关键帧按 `N` 打标签（如"first flash"）；
5. 按 `S` 拍**证据快照**（画面+曲线合成一张 PNG）；
6. `Ctrl+S` 保存分析结果（`.vla`），下次打开视频自动提示加载。

需要看细节：视频上**滚轮**出放大镜；需要循环比对：图表上按 `A`/`B` 打点循环。

### 场景 B：一个监控案件的完整动线

```
新建案件（三·1）→ 素材转码拼接 GO（三·6）→ 产物导入案件归入机位组（三·3）
→ 逐路校时（四）→ 多机同步播放核对（六）→ 没对表的路用同事件对时（六·6）
→ 亮度/音频分析、打标签（五）→ 快照 / 导出选段（七·1、七·2）
→ 画点位图（七·4）→ 生成分析报告（七·5）→ 导出移交包（七·6）
```

每一步在对应章节有详细操作；软件在每一站都有引导提示，拿不准时点界面上的说明文字。

---

## 三、案件与前处理

办案从这一章开始：**建案件 → 拼接素材 → 归入机位组**。

### 1. 新建与打开案件

- **菜单 案件 → 新建案件**：填写名称/调查员/单位/案发日期/城市/区县（必填），**编号自动生成**（`YYYYMMDD-城市区县-x`），创建后固定；案件目录 = `编号-名称`，位于案件根目录下（菜单 案件 → 案件根目录设置 可改，默认 `<程序目录>/cases/`）。
- **起始页**：启动时可选新建/打开/最近案件，或直接进入独立模式（可勾选不再显示）。
- **打开案件**：选择案件目录；若提示锁占用（其他实例正在用或上次未正常关闭），可强制打开。案件菜单的**最近案件**子菜单记录最近 10 个。

### 2. 案件面板（证据树）

打开案件后，左侧出现**案件面板**替代视频列表，分四组：视频 / 前处理会话 / 报告 / 快照。每路视频带徽标：

| 徽标 | 含义 |
|---|---|
| ⏳ | 指纹待计算（后台队列自动补算） |
| ✓ | 指纹一致（源文件与登记一致） |
| ⚠ | 源文件已变更（大小/时间不符） |
| ✗ | 源文件缺失（右键可重新定位） |
| ⏰ | 已完成校时 |
| 📦 | 使用移交包内副本（原路径缺失时自动兜底） |

右键视频：打开播放 / 重新定位 / 批量重新定位 / 移除出案件（默认保留案内数据）/ 计算指纹 / 复制指纹 / 在资源管理器中显示。**双击**打开播放。

### 3. 机位组（案件的组织轴心）

案件里的一切检材都归属**机位组**：每个机位有**自动排序的机位编号**（C01/C02…，创建时分配、永不复用、改名不动），另可加**助记中文名**（如「烟酒店」，随时改）：

- **直接导入的视频**入案时自成新组；**前处理产物**登记完毕时逐产物询问「归入已有摄像头 / 创建新摄像头」（通道名匹配既有组会自动预选）。
- **只有同机位组的文件才能同轴播放**（多机时间线的合并轨按组归并）。
- **案件树按组分层**：📷 组节点 → 成员文件（原件 V### 与产物 P### 同待遇混排，指纹/校时徽标照常）。
- **改名**：组节点右键「机位组改名」——对全组生效，时间线/点位图/报告全系统跟随（改名不动内部键，引用零牵连）。
- **归组调整**：文件行右键「移到机位组 ▸」（现有组或新建组）。
- 老案件打开时自动迁移：同摄像头编号的文件聚为一组，未编号的各自成组。

> 文件编号（V01/P01）是**文件身份**，机位编号（C##）是**摄像头身份**，两套编号独立：点位图上标 C##，文件清单用 V##/P##。

### 4. 视频入案

- 案件打开时，**Ctrl+O 或拖入视频即自动入案**（登记大小/时间、自动排队算指纹）；源视频旁若有已保存的 `.vla`，会询问是否复制导入案件（默认是）。
- 菜单 文件 → **临时打开视频（不入案）**：只想看一眼的文件，与独立模式行为一致。
- 入案后：`.vla` 自动改存 `<案件>/videos/V###.vla`；校时证据帧归入 `<案件>/evidence/`；时间戳框选记忆随案保存。

### 5. 素材转码拼接（前处理）

把一台监控导出的几十段零散录像，整理成**顺序可证明**的连续时间线并拼成一个文件。**支持 DAV（大华）、AVI、WMV、FLV、TS/MTS/M2TS、MOV、MKV、MPG/MPEG、3GP、WebM**——非 MP4 自动统一转码为 MP4（H.264，2 秒关键帧）；参数一致的 MP4 直接无损拼接。

**什么时候用**：一台 DVR/NVR 按时间分段的录像要拼成完整时间线；文件名被重命名、metadata 丢失，只能靠**画面里烧录的时间戳**定序；素材格式杂乱需统一。

**打开**：主窗口工具栏右侧 **「素材转码拼接」** 金色按钮（或 文件 → 素材转码拼接，`Ctrl+M`）。独立任务窗口，与分析界面互不干扰可并行。

#### ① 导入素材（一个 GO 完成）

把视频文件**直接拖进窗口**（或点「添加文件」）。列表行可直接上下拖拽调整顺序（当前顺序即拼接顺序）。

- **自动排除重复**：拖入时按内容去重——尺寸不同必然内容不同（免哈希）；同尺寸计算 SHA-256 指纹比对，一致判重复**保留首个**（状态栏明示排除清单）。
- **「GO」（右下角大按钮，推荐）**：一键完成——**读取每段首尾帧画面时间 → 自动排序 → 拼接出结果**。同一台录像机的一批素材，认出第一段后其余段自动套用同位置识别。
  - 全部认出 → **直接拼接出结果**；片段间时间重叠**自动修剪**（剪后段开头、保前段完整，报告逐段留痕），不打断流程；
  - 个别段没认出 → 按前后段**推算位置**（标「估算」，报告留痕），段数少时直接拼；
  - 拿不准的才停下来给你看（见②）；
  - 拼接方式自动判断：参数一致的 MP4（关键帧 ≤2 秒）**无损直拼**；含非 MP4 或关键帧稀疏 → **自动转码后拼接**；只拖 1 个文件 → 单独转码导出。
- **「直接拼接（不识别时间）」（小字旁路）**：顺序已知时用，最快。**护栏**：文件名全部不含时间信息且未识别排序时，会先弹乱序风险提示，可一键改走 GO。

#### ② 确认顺序（只在拿不准时出现）

顺序可信时本页自动跳过。有疑问时只显示**问题卡**（其余已就绪段折叠）：

- **没认出时间的段**：卡片大字提示 + 首帧截图。点「**框一下画面上的时间**」拖个框 → 同批其余段自动跟着认（同一台录像机只需框一次）；也可「手动输入…」或「按当前位置拼接」。
- **时间重叠的两段**：显示两张截图，点「**交换前后**」一键换位，或「保持现状」。
- **整组没认出时间**：一张组级卡——「框一段画面上的时间，其余段会自动跟着认」。
- 顶部提示「还剩 n 处待确认」，全部处理后主按钮变为「**✓ 确认顺序，继续拼接**」。
- 点「**查看全部片段**」可展开完整时间线与证据卡：每段色块位置=拍摄时间、宽度=时长，缺口灰斜纹、重叠红叠加；双击卡片看大图，拖卡调序，悬停看 OCR 原文。

#### ③ 拼接设置

程序已自动判定拼接方式：**无损拼接（推荐）** 或 **统一格式后拼接**（只转确实需要的文件，原因明示）。

- **硬件编码可选**：高级选项「编码器」下拉——默认 libx264 软件编码，NVIDIA 显卡可选 NVENC、Intel 核显可选 QSV（自动探测可用性）；实际编码器记录在证据报告（取证留档）。
- **时间重叠自动修剪**：检测到重叠默认修剪（剪后段开头、保前段完整），日志与报告逐段留痕；高级选项可改"保留原样"。
- **并行转码**：多组素材组间并行（同组顺序），长任务提速明显。
- **自动命名**：转码产物命名 `<通道>_<日期>_<起止时间>.mp4`（基于校时墙钟）；无校时信息时回退原规则。
- **跨相机混拼归一**：同组分辨率不一致时统一到主导分辨率再拼接（保持宽高比）。

选择输出文件夹（默认素材目录下新建 `LumenArc_Merged_<时间戳>/`），页面显示**预计输出大小与磁盘余量**。点「开始拼接」。

#### ④ 执行与报告

大进度条 + 当前处理段 + 预计剩余时间。完成后结果卡片：

- **证据摘要**（可直接选中复制）：一行结论（导入 N → 排除重复 X → 识别/推算/人工 → 输出 M 个文件）＋**覆盖时间范围**＋**缺口逐条明细**（如 `16:33:17 → 次日 02:53:54（缺 10.3 小时）`）＋重叠修剪/整段丢弃留痕＋排除重复清单；
- **「在主窗口播放输出」** 一键载入校验；**「打开输出文件夹」** 直达产物；**「查看证据报告」** 打开 `report.csv`（每段 OCR 原文、解析时间、文件名、哈希、处理动作）；
- 证据目录 `LumenArc_Evidence_<时间戳>/` 内含首/尾帧截图、拼接清单、操作日志。

> **取证原则**：相机时钟不准时，报告如实呈现原始观测值与偏差说明，程序**绝不静默修正**原始数据。排序结论 = 画面截图 + OCR 原文 + 文件名原文 + metadata 原文 + 人工确认记录。识别失败的片段一律排末尾标 "?"，由人工看图输入时间兜底——证据链不依赖 OCR 精度。

#### 关于 Dahua (.dav) 素材

程序直接支持大华 `.dav`：其内部写有**录制时刻**（不依赖画面识别），排序依据显示「✓ 流内录制时间」；画面时间识别作为交叉校验。`.dav` 为私有格式，拼接前自动统一转换为 MP4（H.264，CRF 18）。转换后的时间与原始录制时间逐段对应，证据报告同时留档流内起始墙钟。

#### 前处理成果导入案件

案件打开时，拼接窗口顶部出现**案件横幅**：默认【导入案件】——拼接输出、报告、证据截图自动登记进案件（`<案件>/preprocess/<时间戳>/`），并逐产物询问归组（见第 3 节）；选【独立输出】则与独立模式一致。

### 6. 指纹与完整性校验

- **自动后台指纹**：视频入案即排队计算 SHA-256（单线程低优先级，不影响播放）；源文件变更后自动重算。
- **完整性校验**（右键单路或全量）：比对登记指纹，报告每路「一致 / 已变更 / 缺失」。快扫（大小/时间）为尽力而为，**确证篡改请以「全部重算」为准**。

### 7. 重新定位（源文件被移动/删除后）

- **单路**：右键 → 重新定位，选新路径（只改引用，不动案件数据）；大小不一致默认拒绝，可显式「仍要采用」（轨迹留档）。
- **批量**：菜单 案件 → 批量重新定位——选候选目录自动按**文件名+大小**模糊匹配，人工确认后**强制指纹比对**：一致即采用，不一致默认拒绝、可显式【仍要采用（留档）】。

### 8. 关闭案件

三处出口等价：案件面板 ✕ / `Ctrl+W` / 状态栏 📁 按钮。有未保存变更时提示保存；**关闭不中断当前播放**，重开案件时自动恢复上次查看的视频与分析现场。

> **取证红线**：源视频永远只读，重定位只改引用路径；「仍要采用」等覆写操作全部留档于 case.json；案内分析数据以 `.vla` 为唯一真源。

---

## 四、校时体系

校时把视频流内时间（00:00 起）对应到真实墙钟，是一切多机对齐与报告时间口径的地基。校时一旦采用，图表时间轴、光标时间、标签悬停时间、CSV 导出全部同步生效，并随 `.vla` 保存。

> 想先弄懂"监控的钟为什么会偏、软件怎么修"：源码仓库 `docs/calibration_explainer/index.html` 有一篇图解科普（浏览器直接打开），上篇纯原理、下篇讲软件做法。

点击工具栏 **「校时…」** 打开校时窗口（非模态：识别/重建在后台进行，可最小化继续播放；关闭窗口不取消任务，重开可见进度与结果）。窗口按场景分两组（均为先预填、点"使用"才生效）：

### 1. 自动校时（推荐）

打开窗口时自动做**秒级快速检查**：文件时间正常会提示"使用 ① 即可"；画面时间与播放进度差异大（疑似抽帧/变速）会**自动进入时间重建**（见第 3 节）。

点「开始自动识别」：自动在**开头/当前位置/结尾**三处读取画面时间，一句话显示结果（如"识别 3 个取样点，画面时钟每天快 31 秒"）；点「查看细节」展开取样点表格（截图/原文/可靠度/⚠ 异常标记），可取消勾选异常点自动重新拟合。

- **不应用漂移修正**：识别结果若含漂移修正，可勾选退回固定偏移。
- 例：三点识别拟合出"流内 0 点 = 2026-07-22 08:15:00，漂移 +40 秒/天"，则时间轴、光标、CSV 均按此换算。

### 2. 对真实时间（北京时间）

监控主机时钟常与北京时间有整体偏差（如从未对时）。三种方式（偏差=北京时间−监控时间，应用到全时间轴/CSV/报告）：

- **方式一 · 校时图片框选（推荐，取证惯例）**：对监控屏幕拍照时把手机标准时间同框拍入，导入照片后**依次框两个框**（框1=监控主机时间、框2=北京时间，均须含秒；框2只框到时分秒时日期取框1同日，跨日自动取较小偏差并在确认卡注明）。OCR 识别后弹**确认卡**：左侧校时图片集成显示、两个框叠加标注（**滚轮放大/拖动平移/双击复位**供逐字校对），右侧两个时间**可直接修改**、偏差行实时重算，原文行只读留证。校时图片、框坐标、OCR 原文随案件存档（图片复制入 `案件目录/calibration/`，元数据写入 `.vla`）。
- **方式二 · 手动输入两个时间**：监控主机时间默认取当前播放位置画面时间，可改，自动算偏差。
- **方式三 · 直接输入偏移量**：「监控主机时间比北京时间 快/慢 X日X时X分X秒」。

可填写说明留档，可随时清除。

### 3. 时间重建（变速/抽帧文件）

部分 DVR/NVR 采用**抽帧录像**：无异动时按 12.5/16/20fps 低帧率录像，有异动切回 25fps；导出时统一按 25fps 重打时间戳，导致"流内时间"被压缩（例如 74 分钟流内对应真实 103 分钟），画面 OSD 时间"走得比播放快"。此类文件无法用单条直线模型校时。

打开校时窗口时的快速检查会自动发现这类文件并**自动进入时间重建**：

1. **粗采样**：全片约每 60 秒取一帧 OCR（约 1~2 分钟）；
2. **分段检测**：自动找出斜率跳变位置（变速边界），剔除 OCR 错读的异常测点（⚠）；
3. **边界加密**：边界附近密集取样，每个边界定位到 ±2 秒内；
4. **音频校验**：用音轨真实时长核对 OSD 总跨度，不一致会明确警告。

完成后展示**分段表**（每段流内范围/斜率/OSD 起点），点「采用重建结果」生效：

| 段 | 流内范围 | 斜率 | OSD 起点 | 说明 |
|----|----------|------|----------|------|
| 1 | 00:00 – 08:20 | 1.000 | 2026-07-22 14:59:43 | 按画面 OSD 重建 |
| 2 | 08:20 – 16:40 | 2.000 | 2026-07-22 15:29:15 | 按画面 OSD 重建 |

- 斜率 1.0 = 正常录像；斜率 2.0 = 画面钟以 2 倍速走动（12.5fps 抽帧导出）；
- 正常录像（无变速边界）会自动判定并提示，行为与自动校时一致，无需等待；
- 测点表中标 ⚠ 的为 **OCR 异常测点**（读数疑似错读，已自动排除），引用其时间时须以证据帧截图为准；
- 重建结果（分段表/测点/异常标记）随 `.vla` 保存，报告中可追溯。

### 4. 前处理继承与缺口显示

用"素材转码拼接"输出的文件随附 `.lumencal.json` 校时信息，打开时自动继承为**分段校时**——每段时间轴按其画面 OCR 时间锚定（感知重叠修剪/丢弃/转码失败，转码段按产物实测时长锚定）；段间时间缺口处墙钟精确跳变（监控常态），打开时弹出缺口提示（该提示也会写入报告）。

**缺口在时间轴上的显示**：时间轴保持播放连续（流内坐标），刻度只标注**真实有素材**位置的墙钟；每个缺口处画**红色虚线竖线**并标注红色小字「缺 10.3h」。点击缺口位置 seek 自动**跳到缺口后第一段**。缺口红字与刻度重叠时自动只留红虚线（刻度优先），放大到不重叠时红字恢复。

### 5. 多机之间的时间接力（同事件对时）

某路监控完全没对过表时，可以在**多机同步播放**窗口里用「同事件对时」：找两路画面里"同一件事的同一瞬间"打标记，软件算出这路的钟差并写入案件（含取证链）。原理属于本章校时体系，详细操作见 **第六章第 6 节**。

---

## 五、单路分析

### 1. 播放与控制

| 操作 | 方式 |
|------|------|
| 播放 / 暂停 | `空格` 或 `k`，或工具栏播放/暂停按钮 |
| 前进/后退一帧 | `→` / `←` |
| 音量 | `↑` / `↓` |
| 倍速播放 | 工具栏倍速按钮，循环 `0.25x → 0.5x → 1x → 2x → 4x → 8x`（音频变速，音调随速度变化） |
| 加速/减速一档 | `c` 或 `l` / `x` 或 `j` |
| 恢复 1x | `z`；停止按钮也会重置倍速 |
| 帧级跳转 | 拖拽折线图中的橙色竖线，视频实时同步 |

> 播放类按钮在未打开视频时为灰色禁用，打开后自动启用。

### 2. 画面调节（亮度 / 对比度 / 旋转）

工具栏「**画面调节**」打开浮窗面板：

| 控件 | 说明 |
|------|------|
| 亮度 / 对比度 | 拖滑杆（±50），暂停时也实时预览 |
| 伽马 | 0.30 ~ 3.00；>1 提亮暗部而不冲淡高光，夜间/昏暗素材常用 |
| 黑点 / 白点 | 色阶拉伸，应对烟雾/雾低对比素材 |
| 反色 | 负片效果（某些高光场景更易辨认） |
| 旋转 | 「⟳ 顺时针 90°」按钮，0° → 90° → 180° → 270° 循环 |
| 复位 | 一键全部回默认 |

**要点**：
- 调节只影响**显示与快照**；亮度分析、ROI 数据、语谱图与证据文件始终保持原始画面不变。
- 主画面、放大镜、时间戳钉图、副屏全屏各处视图应用同一组调节，所见一致。
- 旋转时 ROI、多边形、辅助线、时间戳框选、放大镜、截图叠加**随画面一起转**；新画的框自动换算回原视频坐标存储与分析，证据数据始终对应同一物理位置。
- 每个视频的记忆独立：切换视频后各自的调节与旋转档位自动恢复。

### 3. 副屏全屏显示（多屏主机）

多屏主机上可把视频原画面送到另一块屏幕全屏展示（汇报/观察用），主屏继续正常操作。

- **开启**：「视图 → 副屏全屏显示」菜单里点选目标屏幕；或按 `F11` 直接使用上次选择的屏幕（首次默认选主窗不在的那块）。
- **内容**：纯视频画面，不带 ROI 框、放大镜等任何叠加；画面调节与主画面**实时同步**。
- **退出**：按 `ESC` 或双击画面；鼠标静止 3 秒自动隐藏光标。
- **换屏**：先退出再在新屏幕上重开（菜单里标有「全屏中」的是当前屏幕）。
- 单屏机器菜单项置灰；清数据/关闭文件时副屏窗自动关闭。

### 4. ROI 区域与辅助线

工具栏三个互斥模式按钮：**矩形**（拖拽绘制分析区域）、**多边形**（单击加顶点、双击闭合，快捷键 `P`）、**辅助线**（参考线，不参与分析，快捷键 `G`）。

| 操作 | 方式 |
|------|------|
| 绘制 | 矩形：左键拖拽；多边形：单击顶点、双击闭合（≥3 点）、右键取消 |
| 选中 | 点击已有区域（边框变亮、四角出白色调整点；重叠时选中最上层） |
| 移动 | 选中后拖拽区域体（多边形也在矩形模式下拖拽移动） |
| 调整大小 | 拖拽四角白色调整点 |
| 删除 | 选中后 `Delete`，或直接右键点击区域（无需先选中） |
| 复制/粘贴 | `Ctrl+Shift+C` / `Ctrl+Shift+V`（可跨视频粘贴） |
| 辅助线 | 左键拖拽画线；`Shift`+拖拽约束水平/垂直；拖端点调整；右键删除 |
| 退出模式 | `Esc` |

- 支持多个 ROI，自动分配颜色（赤橙黄绿青蓝紫），标记 R1、R2…
- **数据保护**：已跑过分析再调整 ROI，会弹确认："调整该区域将导致亮度量化数据失效"。确定=清数据允许调整；取消=恢复原位。

### 5. 亮度量化分析

点工具栏蓝色「**亮度分析**」，逐帧扫描视频计算每个 ROI 的平均亮度（全帧率逐帧，47 分钟 1440p 约 40 秒）。

| 状态 | 说明 |
|------|------|
| 前置条件 | 已打开视频 + 至少 1 个 ROI |
| 进度 | 状态栏显示百分比，可随时取消 |
| 后台运行 | 分析低优先级后台跑，**期间可正常播放、标注、操作** |
| 完成 | 折线图显示各 ROI 亮度曲线（Y 轴 0~255），右侧 Y 轴可叠音量曲线 |
| 自动缓存 | 完成后自动生成 `.vla` 缓存，下次打开直接加载 |

Y 轴自动范围默认开启（右键菜单可关）。

### 6. 音频分析

独立于亮度分析，无需画 ROI。点工具栏青色「**音频分析**」：提取音轨 → 音量曲线（折线图右侧 Y 轴，绿色，单位 dB）+ 语谱图（下方独立面板）。

**语谱图操作**：

| 操作 | 方式 |
|------|------|
| X 轴缩放（时间轴） | 滚轮，以鼠标位置为中心 |
| Y 轴缩放（频率） | `Ctrl + 滚轮`，以鼠标位置为中心 |
| X 轴平移 | `Alt + 滚轮`（上滚向过去，每格 10% 可视宽度）或中键拖拽 |
| 频率轴平移 | `Ctrl + 左键拖拽` |
| 重置频率范围 | 双击 Y 轴区域 |
| 双向同步 | 图表与语谱图时间轴互相同步跟随（滚轮操作同样联动） |

**降噪（两条链路，同一算法家族：内置谱门控，零外部依赖）**：

| 链路 | 操作 | 作用 |
|------|------|------|
| **分析显示降噪** | 语谱图标题栏「降噪」滑杆（0.0 关 ~ 5.0；1.0=标准，≥2.0 强）+「应用」重跑分析（调回 0 再应用 = 复原） | 语谱图/音量曲线的显示更干净（底噪谱整段统计 + 增益掩码） |
| **播放监听降噪** | 菜单 设置 →「播放音频降噪」开关（默认关），强度跟随上述滑杆 | 播放声音实时降噪（流式谱门控 + 自适应底噪跟踪；scrub 拖拽片段旁路；seek 自动重建）；多机窗口各引擎同步生效 |

**取证红线**：两种降噪都只作用于"显示/监听"，**源文件、分析原始数据、导出产物一律不动**。播放降噪的音画对齐：DSP 计算滞后（≤42.7ms）由 1 秒设备缓冲吸收，稳态零音画偏移。

### 7. 图表操作与标签

**缩放平移**（与语谱图滚轮操作完全一致）：
| 操作 | 方式 |
|------|------|
| X 轴缩放（时间轴） | 滚轮，以鼠标位置为中心 |
| Y 轴缩放（亮度） | `Ctrl + 滚轮`，以鼠标位置为中心（首用自动关闭 Y 自动范围；右键菜单可恢复） |
| X 轴平移 | `Alt + 滚轮`（上滚向过去，每格 10% 可视宽度） |
| 平移（拖拽） | 中键拖拽 |
| 跳转视频 | 点击/拖拽橙色竖线 |

**右键菜单**：全部适应 / X 轴适应 / 刻度显隐 / Y 轴自动范围 / 添加·删除标签 / A-B 区域操作 / 图表辅助线（水平·垂直·删除）。

**时间轴刻度**：起止加粗标注、主次刻度线、步长按可见范围自动选择（1 秒 ~ 6 小时）；光标上方实时显示当前时间（校时后为北京时间）。

**标签**：

| 操作 | 方式 |
|------|------|
| 添加 | 双击图表输入文字选颜色（6 预设色），或按 `N` 在当前播放位置快捷添加 |
| 跳转 | 单击标签，视频跳到标签时刻 |
| 锚定 | 标签锚定添加时的时间位置，缩放平移自动跟随 |
| 删除 | 右键 → 删除光标处标签 |

### 8. A/B 区域循环

| 操作 | 方式 |
|------|------|
| 设置 A / B 点 | `A` / `B` 键，或右键菜单 |
| 切换循环 | 右键"开启/关闭循环"（循环=到 B 跳回 A；非循环=到 B 暂停） |
| 清除 / 缩放到区域 | 右键菜单 |

设置 A/B 后图表自动缩放到区间，清除后恢复全轴。A/B 也是**导出选段**的区间（见第七章第 2 节）。

### 9. 放大镜

视频画面上**滚动滚轮**自动创建；`Esc` 或右键"关闭放大镜"关闭。

| 操作 | 方式 |
|------|------|
| 缩放 | 滚轮，以鼠标位置为中心（1.5x ~ 10x），暂停时同样生效 |
| 平移 | 中键拖拽（主视频与放大镜内均生效，暂停可用）；右键可开"反向平移" |
| 视口比例 | 拖拽主视口与放大镜间的分割线 |

- **放大镜内操作 ROI**：直接在放大视图内绘制、移动、缩放 ROI，坐标自动映射回主视频全尺寸坐标。
- **来源标识框**：放大镜开启期间，主画面以金色四角括号 + 倍率徽章（如 `2.0×`）实时标出放大区域；跟随光标/缩放/平移/旋转同步更新。标识框不参与鼠标交互，且会随证据快照一并烧录（证据自解释：放大图从哪来）。
- **截图叠加同步**：已放置截图叠加时，放大镜内自动显示裁剪后的叠加效果。

### 10. 截图叠加

截取视频帧或加载外部图片，半透明叠加到当前画面对比。

| 操作 | 方式 |
|------|------|
| 截取 | 工具栏「截取」当前帧，左侧出缩略图窗口 |
| 加载外部图片 | `文件 → 加载图片为叠加`（PNG/JPG/BMP/TIFF） |
| 编辑 | 点缩略图展开面板：亮度/对比度/透明度滑块 |
| 放置/取消 | 工具栏「放置」（激活时橙色背景）或面板打勾钮，两者同步 |
| 清除 | 面板垃圾桶钮 |

### 11. 时间戳提取

固定视频中的一个区域，实时显示在独立浮动窗口（持续观察某区域，如 OSD 时钟）：

- **创建**：`Ctrl+Shift` + 左键在视频上拖框；
- **关闭**：关掉浮动窗口即可。窗口始终置顶。

### 12. 视频列表（独立模式多视频管理）

| 操作 | 方式 |
|------|------|
| 添加 | 面板底部「添加」，或拖入面板 |
| 移除 / 清空 / 排序 | 选中后操作；拖拽调整顺序 |
| 切换当前视频 | 双击列表项 |
| 收起/展开 | 面板标题栏箭头 |

各视频的 ROI/调节/校时各自独立记忆。

---

## 六、多机同步播放

多路监控按**真实时间**对齐同屏播放，是多机案件核对现场的主战场。

**进入**：菜单 案件 → 多机同步播放（案件开着即可进）；无案件时也可 文件 → 多机对比播放（2 路临时）。窗口**默认最大化**；主视口若正在播放会自动暂停，多机每次起播前也会再拦一次主视口（防双路音频/资源竞争）。

### 1. 勾选机位（第一步）

开窗先给清单：案内全部视频与前处理产物逐路列出，校时状态实时读取：✅ 已校时 / ⚠ 未校时 / ❌ 文件缺失（不可选）。已校时路默认勾选（≤4），点「**开始同步播放**」：

- 2~4 路且全部已校时 → **合并时间线模式**；
- **同机位多文件可并成一路（合并轨）**：清单顶部自动列出「⊞ 机位标签 · N 段合并轨」组行（同机位组 ≥2 个文件且全部已校时才出现；任一未校时则整组不出现——先补齐校时）。勾选组行即把该机位多个文件并成**一路**：引擎自动跨段换文件，时间线行内按段多色块显示，段重叠时"先起步的段赢"，被压住的重叠区画斜纹标 ⚠；瓦片名称带段标 [k/N]。合并轨与单文件路可混选（组算 1 路，总计 ≤4 路）。勾选组行后其成员行自动禁选，反之亦然。注意：合并轨会话暂不支持「导出选段」（请把各段分别作单路加入再导出）。
- 2 路且含未校时 → **分开进度条模式**（临时对齐）；
- 3 路以上含未校时 → 开始钮禁用并提示（点该行「**去校时…**」到主窗完成校时，回来「**刷新清单**」；或减到 2 路走临时对齐）；
- 播放中可点「**↩ 重选机位**」返回清单换路。

### 2. 合并时间线模式

底部条图每路一行，色块 = 该机位**墙钟覆盖**（按真实时长，由各自校时换算）：红色叠加 = 多机同时覆盖，灰色斜纹 = 缺口。**拖动游标**全路实时追逐同步走带；游标顶部金色气泡实时显示当前墙钟时刻（MM-dd HH:mm:ss）。

- **已对时标识**：完成北京时间对时（直接照片/OCR，或经同事件对时间接接力）的路——瓦片左上角挂绿色「**✓ 已对时**」角标、时间线行标签下挂绿色「**✓ 已对时·北京时间**」。没挂标识的行 = 仅按监控 OSD 墙钟对齐，未对过真实时间。
- **北京时间偏移全程生效**：做过「对真实时间」的路，合并时间线/瓦片 OSD/游标报时一律按北京时间口径；未做的路仍按各自 OSD 时间。

### 3. 分开进度条模式与临时对齐

2 路且含未校时路时：每路各自带时间的进度条，纯画面复勘（无亮度/音量/语谱面板）。

- **对齐前两路各自独立**：拖进度条只动本路，时间口径各自流内时间；
- **确认对齐后两路联动**：拖任一路同步走带，临时路时间行附「≈墙钟」推算参考；
- **临时进**：未校时路手动对一个时间点（画面或声音事件）临时加入同步——**会话级，不落盘、不写案**，关窗即失效，瓦片以「临时对齐」黄角标明示；对齐时点「对齐…」分别拖两路到同一时刻（点瓦片切换听音，以声定点）再「确认对齐」。

> 对齐精度取决于各路校时精度；临时进路为手动对齐，精度更低，角标单独明示。

### 4. 瓦片操作

| 操作 | 方式 |
|------|------|
| 切换发声路 | 单击瓦片（🔊） |
| 独立放大镜 | 悬停瓦片滚轮（1x~8x），中键拖拽平移 |
| 回主窗单路分析 | 双击瓦片或时间线色块 |
| 画面调节 | 工具行「画面调节」浮窗——亮度/对比度/伽马/色阶/反色/旋转，**只作用于当前选中瓦片**（点击瓦片切换对象，选中者有强调色外框；每路独立记忆；仅显示链路，引擎原始帧与证据链不变） |
| OSD 角标 | 一键开关 |

多路高负载时自动性能治理：优先硬解，超重路自动降「预览降清档」（仅预览降清，不碰原始数据），状态栏提示不强制。

### 5. 多机选段导出

合并时间线上按 `A`/`B` 在游标处打点（`Ctrl+A` 清除），工具行「导出选段」→ 与单路同款分段变速对话框（墙钟域显示），合成**全机位宫格 + 各路覆盖条 + 移动游标**的 1080p MP4；某时刻无机位画面的格自动显示「该时刻无画面」占位；音轨取自当前主听机位（未全程覆盖选段时产物无音轨并在完成提示注明）。

### 6. 同事件对时（给没对过表的机位修钟）

**一句话原理**：某路视频没校过时间？在两路画面里找到「同一件事的同一瞬间」告诉软件，它就能算出这路的钟差多少——找到 1 次能修**整体偏差**；找到 2 次还能修**越走越快/慢**（第 2 次自愿，不强制）。

多机同步播放页工具行「同事件对时」进入。右侧面板分 ④ 段，跟着顶部横幅走（横幅永远显示"现在该干嘛"）：

1. **① 谁的钟是准的？** 选已校时机位（经别的机位接力对过的会注明）；
2. **② 要修谁的钟？** 选没校时的机位；
3. **③ 打「同一瞬间」标记**：找一件两路都看得见的事（同一辆车过门口、同一声喇叭、有人挥手、灯亮灯灭）。沙盒内两个高亮播放按钮：「**▶ 播放选中瓦片**」只播当前点选的那一路（其余暂停，逐帧比对单路用）；「**▶▶ 播放全部**」两路一起播（配合「同听两路」比对声音事件）；顶部工具栏「播放」钮在沙盒内 = 播放全部。用「微调画面」逐帧把两边都对到那个瞬间，**给这件事起个名**（必填，报告里要念出来），点「📍 就是这一瞬！打标记」；
4. **④ 预览与保存**：每记下一个标记，列表立刻用大白话显示结论（如「目标的钟慢 2 分 14 秒」）。点「▶ 应用预览」播放试看——两路贴齐了点「💾 保存校时」。预览后每个标记附**对时误差**，超过半秒会红字提醒「这对标记可能找错事了」。

保存时弹**取证链确认卡**：逐级列出从绝对校时锚到本路的完整传递链（每跳事件名/两路时刻/容差）+ 累积容差声明，确认后写入案件留档。

规矩：多跳接力允许（C 可参考接力对过的 B），**成环禁止**（链成环无法陈述，打标记时自动拒收）；间接对时可信度天然低一档（最高 0.8），瓦片/确认卡如实标注「间接·N 跳·累积容差 ±Xms」。取证链随校时一体存入 `.vla`。

---

## 七、产出与移交

### 1. 证据快照（工具栏「快照」/ `S`）

一键把当前分析现场合成为一张竖向全宽 PNG（有几段拼几段，黑底无留白）：

| 段落 | 内容 |
|------|------|
| 视频帧 | 所见即所得（旋转+调节后画面）；烧录 ROI/多边形/辅助线（按模型原色+序号标签）；左下 OSD 烧录文件名、当前帧标签（±1s 内最近）、北京时间（已校时）或相对时刻。**开启放大镜时视频帧为左右分屏**：左半原生全分辨率原帧+金色四角括号标出源区域，右半放大视图同高（标注同步烧录进放大坐标系）；画幅=左宽+右宽，细节零损失 |
| 亮度+音量曲线 | 有分析数据时合成；离屏矢量重渲染为全宽横幅（曲线锐利不压扁），含橙色时间光标 |
| 语谱图 | 有音频分析时合成；与屏幕同一色标/视窗，含时间光标 |
| 放大镜视图 | 仅当放大镜开启且未走分屏（无有效源区域）时单列；分屏模式下已融入右半 |

各段之间有金色竖条分段标题。

**保存位置**：开案件时存 `案件目录/snapshots/`（案件面板快照组即时可见）；未开案件存视频同目录 `snapshots/`。文件名 `视频名_校准后北京时间.png`（未校时为 `视频名_tHH-MM-SS.png`），重名自动避让。旋转档位写入 PNG 元数据并在保存提示中注明。

**软件内预览**：案件树快照条目带缩略图；双击（或右键「预览」）打开图片预览窗口——打开即自适应窗口；滚轮缩放（光标锚点）/拖动平移/双击复位/「1:1」/「在资源管理器中显示」。校时照片、证据帧等图片同样用该预览器打开。

### 2. 导出选段视频

A/B 选段存在时，工具栏「**导出选段**」可用：把选段合成为单个 MP4（1920×1080）——上方视频画面、下方亮度曲线与语谱图（各带移动游标，未跑分析的面板自动隐藏）。导出面板为**非模态浮窗**：打开后播放与游标照常操作，「在当前游标处分段」实时取播放位置；进度内嵌面板、可最小化。

- **分段变速**：选段内的标签（`N` 键）自动成为分速边界初值；对话框中可逐段调倍速（0.25x~8x）、在游标处再分段或删除分段点——不关键的段快放掠过，关键段常速/慢放。方案随 `.vla` 持久化。
- **标签入导出**：选段内的标签在导出视频的曲线条上打同色竖标+文字；播到标签时刻起，画面 OSD 额外烧录一行「🏷 标签内容」（标签色），**显示 5 秒后自动隐去**（多标签重叠取最新一个）。
- **图表光标**：导出视频的曲线/语谱游标随输出时间动态移动，顶部带三角柄。
- **放大镜入导出**：导出时主窗放大镜开着（或多机各瓦片有滚轮放大），导出画面右下角嵌入「放大镜 ×N」子窗口（金框），导出时刻的放大取景随帧呈现。
- **完成后一键定位**：面板出现「📂 打开所在文件夹」，直接在资源管理器中选中产物。
- **信息角标可选**：勾选后烧录「演示副本 · 当前倍速 · 时刻（已校时=北京时间）· 案件号」，随分段变速动态更新。
- 音频按分段自动变速随影；源文件只读不动，产物命名 `LAClip_<原名>_<A>-<B>.mp4`，案件模式默认存案内 `exports/`。

### 3. 数据导出与保存

| 功能 | 路径 | 格式 |
|------|------|------|
| 导出 CSV | `导出 → 导出为 CSV` | 亮度数据 `xxx.csv` + 标签数据 `xxx_labels.csv` |
| 保存分析结果 | `文件 → 保存分析结果`（`Ctrl+S`） | `.vla`（含 ROI + 亮度 + 音频 + 校时 + 标签 + 截图融合 + 辅助线） |
| 加载历史结果 | `文件 → 加载分析结果`（`Ctrl+L`） | `.vla` |

`.vla` 为单文件自包含格式（频谱内嵌、二进制压缩），可随 U 盘/网络跨设备传递分析成果。高版本生成的 `.vla` 无法在低版本软件中打开；旧版 `.vla`（含 `.spec` 伴随文件）在当前版本中均可正常读取。

### 4. 监控点位图编辑器

案件菜单「编辑监控点位图」——导入现场平面图/地图截图，布监控点位，一键生成**带标准图框**的成品示意图（2480×1754 PNG 存案内 `reports/assets/sitemap.png`），报告「监控点位图」节自动嵌入。

- **导入底图**：选图后弹**裁切对话框**——拖框保留有效区域（或整张使用），确认后图片复制入案并**固定适配画布**（不再平移缩放，防误挪）。
- **布点**：左侧机位**按机位组分组**（同一监控的原件与拼接产物聚为一组，一组一行），拖到画布落点；同机位再拖=挪位；组名未自定义时附源文件名提示，悬停看成员全清单。
- **调方向/大小**：点选点位 → **滚轮转朝向、Shift+滚轮调张角、Alt+滚轮调标注字号**（50%~300%；画布底部有横幅提示），或属性条精确输入朝向/张角/半径/字号；扇形=拍摄方向与覆盖范围，标注字色与扇形同色（机位色与多机时间线一致）。
- **图上标注 = 机位编号**（C01/C02…；助记名只在左侧栏/案件树看，图面保持最短）。
- **出图保存**：双图框+右下角标题栏（案件编号/图名/制图/审核/日期/图号）。
- 机位从案件删除后，对应点位标红「已移除」不自动删（人工决定去留）；机位改名图上标签自动跟随。
- 一案一张，坐标归一化存 `sitemap.json`（换底图不失位）。

### 5. 生成分析报告

案件菜单「生成分析报告」→ **自检与补录对话框** → 生成 **DOCX 草稿**到案内 `reports/火灾视频分析报告_时间戳.docx`。

- **① 生成前自检**：❌ 阻断项（源文件缺失/全部未校时）须先处理；⚠️ 提示项（审核/批准人未填、点位图未绘、无标签节点等）可放行，报告相应位置留白。
- **② 信息补录**：审核人/批准人/送检人 + 逐路拍摄方向/提取方式/存储介质，保存入案件（下次生成自动记忆）。
- **自动填入**：封面（案件编号/火灾名称/单位/分析人/日期）、目录、基本情况（案情=案件备注）、检材清单表与逐视频物理属性（格式/大小/分辨率/帧率/编码/时长/**MD5+SHA-256 双哈希**，生成时自动补算）、时间校准结果表（显示时间/校时方式/时间差/校准公式）、校准证据截图、**接力对时取证链**（逐跳事件名+容差+累积容差声明）、火势发展时间表与关键节点骨架（来自图表标签，北京时间口径）、分析意见（起火时间由最早标签预填「不晚于 …」）、局限性说明（OSD 疑似错读点自动列出）、附件（快照关键帧嵌入 + 导出片段清单）、落款签名盖章留白位。
- **拼接记录列为证据**：前处理拼接产物在报告中列出产物↔来源文件全表（来自案内 preprocess/ 证据目录的 report.csv），附 operations.log 关键决策行与原件路径引用。
- **留白待手填**（DOCX 可编辑）：画面描述/分析结论/起火部位/人物车辆活动等人工撰写位。
- 「分析过程」节自动嵌入逐路**亮度变化曲线图**（横轴北京时间；需已做亮度分析）。
- 大文件哈希补算在点「生成报告」后带**进度条**进行（可取消即弃稿）；自检/补录阶段秒开。

### 6. 导出移交包

案件菜单「导出移交包」：

- **完整包（默认）**：案件目录 + `sources/` 视频副本——**换机零操作可用**（原路径缺失时自动使用包内副本，徽标 📦）；
- **轻量包**：不含视频副本，接收方需自行准备源视频并重新定位；
- 导出前自检：缺失指纹可即补、缺失源可即定位、磁盘空间预检（不足阻断）；包内含 `README.txt`（案件信息+指纹清单）与 `manifest.json`（完整性清单），导出后自动快速校验副本一致性。

---

## 八、附录

### 键盘快捷键一览

| 按键 | 功能 |
|------|------|
| `空格` / `k` | 播放 / 暂停 |
| `←` / `→` | 后退一帧 / 前进一帧 |
| `↑` / `↓` | 音量 +5 / -5 |
| `c` / `l` | 加速一档 |
| `x` / `j` | 减速一档 |
| `z` | 恢复 1x 倍速 |
| `N` | 在当前播放位置添加标签 |
| `A` / `B` | 设置 A 点 / B 点 |
| `S` | 证据快照 |
| `P` / `G` | 多边形模式 / 辅助线模式 |
| `Esc` | 关闭放大镜 / 退出当前模式 |
| `Delete` | 删除选中的 ROI / 辅助线 |
| `右键` | 删除鼠标下的 ROI / 辅助线（无需先选中） |
| `Ctrl+O` | 打开视频（案件模式=入案） |
| `Ctrl+M` | 素材转码拼接 |
| `Ctrl+W` | 关闭案件（仅案件模式） |
| `Ctrl+S` | 保存分析结果 |
| `Ctrl+L` | 加载分析结果 |
| `Ctrl+Shift+C` / `Ctrl+Shift+V` | 复制 / 粘贴 ROI |
| `Ctrl+Shift`+拖框 | 时间戳提取 |

> 程序内 **帮助 → 快捷键速查** 可查看完整列表（半透明弹窗）。

### 常见问题

| 问题 | 解决方法 |
|------|---------|
| **Win10 打开软件闪退 / 无法启动** | 安装随包附带的 **`vc_redist.x64.exe`**（微软 VC++ 运行库安装包，与软件同目录）：Win10 老系统常缺该运行库，装一次永久生效；装完重新打开软件即可 |
| 打开视频黑屏 | 确认格式支持；DAV 等私有格式先用「素材转码拼接」处理 |
| DAV 格式无法直接打开 | DAV 是大华私有格式，在「素材转码拼接」中导入即可自动转 MP4 |
| 亮度分析按钮灰色 | 确认已打开视频并绘制了至少一个 ROI |
| 音频分析后没有音量曲线 | 确认视频包含音轨；音频分析无需绘制 ROI |
| 截图叠加不显示 | 需先「截取」（或加载图片），再「放置」才叠加到视频 |
| 放大镜内 ROI 不生效 | 直接在放大镜视图内绘制/调整 ROI，坐标自动映射回主画面 |
| `.vla` 文件打不开 | 高版本生成的 `.vla` 无法在低版本中打开，请升级软件；旧版 `.vla` 在当前版本均可读取 |
| 多机同步开始按钮禁用 | 3 路以上须全部已校时；按清单提示点「去校时…」逐路完成，或减到 2 路走临时对齐 |
| 合并轨组行不出现 | 同机位组需 ≥2 个文件且全部已校时；先补齐校时再刷新清单 |
| 报告生成被阻断 | 自检 ❌ 项须先处理（源文件缺失 → 重新定位；全部未校时 → 先校时） |
| 使用中异常退出 | 软件已自动保存诊断信息；下次启动会提示目录位置（`%LOCALAPPDATA%\LumenArc\LumenArc\crash`），请将该目录内容发送给开发者 |

### 相关文档

| 文档 | 位置 | 说明 |
|------|------|------|
| 校时科普图解 | 源码仓库 `docs/calibration_explainer/index.html` | 浏览器打开：监控时钟为什么会偏、软件怎么修 |
| 版本更新记录 | 源码仓库 `CHANGELOG.md` | 各版本功能演进史 |
| 操作手册源文件 | 源码仓库 `MANUAL.md` | 本手册的 Markdown 源（PDF 由工具链生成） |
