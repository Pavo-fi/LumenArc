# 追光者 Lumen Arc v0.5 — 操作手册

---
本项目由广东省火调技术中心发布并维护，采用Apache许可证2.0版进行授权。
---

## 一、软件概况

**追光者 Lumen Arc** 是一款视频亮度分析桌面工具。它能对视频画面中的指定区域逐帧计算平均亮度，绘制亮度随时间变化的折线图，并提供多种辅助工具帮助你观察和导出分析结果。

### 启动与打开视频

双击 `LumenArc.exe` 启动程序。打开视频的方式有三种：

| 方式 | 操作 |
|------|------|
| 菜单打开 | `文件 → 打开视频`，或按 `Ctrl+O` |
| 拖放打开 | 直接把视频文件拖入程序窗口 |
| 自动加载缓存 | 打开视频时如果同目录有已保存的 `.vla` 分析结果，会提示是否直接加载，从而实现**分析结果通过vla文件跨设备传递** |

支持格式：MP4、AVI、MKV、MOV、WMV、FLV、WEBM。

### 播放控制

| 操作 | 方式 |
|------|------|
| 播放 / 暂停 | 单击视频窗口获取焦点后，按 `空格` |
| 前进一帧 | 按 `→` |
| 后退一帧 | 按 `←` |
| 音量增大 | 按 `↑` |
| 音量减小 | 按 `↓` |
| 倍速播放 | 点击工具栏倍速按钮，循环切换 `0.25x → 0.5x → 1x → 2x → 4x → 8x` |
| 帧级跳转 | 拖拽折线图中的青色竖线，视频实时同步到对应位置 |
| 停止播放 | 点击停止按钮（倍速自动重置为 1x） |
| 加速一档 | 按 `c` 或 `l` |
| 减速一档 | 按 `x` 或 `j` |
| 恢复 1x | 按 `z` |
| 播放/暂停 | 按 `空格` 或 `k` |

> 播放按钮（播放、暂停、停止、倍速、分析、设置时间、截取）在未打开视频时为灰色禁用状态，打开视频后自动启用。

### 六大功能概述

| 功能 | 说明 |
|------|------|
| 亮度量化分析 | 框选视频区域，自动逐帧计算亮度并绘图 |
| 放大镜 | 局部放大查看视频细节，支持在放大镜内操作 ROI |
| 截图叠加 | 截取视频帧进行半透明叠加对比 |
| 时间戳提取 | 固定一个区域，实时显示在浮动窗口中 |
| 时间设置 | 将图表时间轴与真实世界时间对应 |
| 轴标签 | 为折线图添加文字标记，标注关键时刻 |

### 中英双语

通过菜单 `帮助 → 语言` 切换中文 / English，切换后程序自动重启。

### 工具栏按钮状态

工具栏按钮在不同状态下会显示不同样式：

- **灰色禁用**：功能不可用（如未打开视频时的播放按钮）
- **橙色边框**：功能已启用（如打开视频后的"截取"按钮）
- **橙色背景**：功能已激活（如已放置叠加时的"放置"按钮）
- **蓝色背景**：主要操作按钮（"分析"按钮）

---

## 二、亮度量化分析

### ROI 区域操作

ROI（Region of Interest，感兴趣区域）是你希望分析亮度的视频区域。

| 操作 | 方式 |
|------|------|
| 绘制区域 | 在视频画面上按住鼠标左键拖拽，画出矩形框 |
| 选中区域 | 点击已有区域（选中后边框变亮，四角出现白色调整点） |
| 移动区域 | 选中后拖拽区域体 |
| 调整大小 | 选中后拖拽四角的白色调整点 |
| 删除区域 | 选中后按键盘 `Delete` |

- 支持多个 ROI，每个自动分配不同颜色（赤、橙、黄、绿、青、蓝、紫），标记为 R1、R2、R3...
- 重叠区域点击时选中的是最上层的区域。

**数据保护：** 如果已经运行过分析产生了数据，此时再调整 ROI 区域，会弹出确认提示："调整该区域将导致亮度量化数据失效。确定要继续吗？"

- 点"确定"：清除已有分析数据，允许调整
- 点"取消"：不调整，恢复区域原位

### 运行分析

点击工具栏蓝色 **"分析"** 按钮，程序会自动逐帧扫描整个视频，计算每个 ROI 的平均亮度。

| 状态 | 说明 |
|------|------|
| 前置条件 | 已打开视频 + 至少画了 1 个 ROI + Python 已安装（运行 `setup_python_deps.bat` 安装） |
| 进度显示 | 弹出进度对话框，显示已分析帧数和百分比，可随时取消 |
| 完成 | 弹出报告：总数据点数和区域数，下半部分折线图显示亮度曲线 |
| 自动缓存 | 分析完成后自动在视频同目录生成 `.vla` 缓存文件，下次打开时可直接加载 |

分析完成后，折线图的 Y 轴表示平均亮度值（0 ~ 255，0 为黑色，255 为白色），X 轴为时间。如果开启了 Y 轴自动范围（默认开启），图表会自动缩放 Y 轴以适应数据范围。

### 数据导出

| 功能 | 路径 | 格式 |
|------|------|------|
| 导出为 CSV | `导出 → 导出为 CSV` | 亮度数据 `xxx.csv` + 标签数据 `xxx_labels.csv` |
| 保存分析结果 | `文件 → 保存分析结果` | `.vla`（含 ROI + 亮度数据 + 时间偏移 + 标签 + 截图融合） |
| 加载历史结果 | `文件 → 加载分析结果` | `.vla` |

> **注意：** 高版本生成的 `.vla` 文件无法在低版本中打开，反之 低版本的文件在高版本中可以正常兼容加载。

---

## 三、放大镜

放大镜用于查看视频局部细节，停靠在窗口右侧。

| 操作 | 方式 |
|------|------|
| 创建放大镜 | 在视频画面上滚动鼠标滚轮，自动创建 |
| 缩放 | 滚轮滚动，以鼠标位置为中心（1.5x ~ 10x） |
| 平移视图 | 按住鼠标中键拖拽（主视频和放大镜内均生效） |
| 关闭放大镜 | 按 `Esc`，或在视频画面右键选择"关闭放大镜" |

**缩放：** 可以通过拖拽主视频视口和放大镜视口间的框线调整视口之间大小，从而获得最佳观看体验。

**放大镜内操作 ROI：** 放大镜锁定后，可以直接在放大镜视图内绘制、移动、缩放 ROI，坐标会自动映射回主视频的全尺寸坐标。

**缩放级别显示：** 放大镜右上角显示当前缩放倍数（如 `2.0x`）。

**截图叠加同步：** 如果已放置截图叠加，放大镜内会自动同步显示裁剪后的叠加效果。

---

## 四、A/B 区域循环播放

在图表上设置 A/B 两点，可在两点之间单独播放或循环播放。

| 操作 | 方式 |
|------|------|
| 设置 A 点 | 按 `A` 键，或右键菜单"设置 A 点" |
| 设置 B 点 | 按 `B` 键，或右键菜单"设置 B 点" |
| 自动循环 | 设置 A/B 后自动开启循环播放 |
| 切换循环 | 按 `L` 键切换循环开/关 |
| 清除 A/B | 右键菜单"清除 A/B 区域" |
| 缩放到区域 | 右键菜单"缩放到 A-B 区域" |

**循环模式：** 到达 B 点自动跳回 A 点继续播放。

**非循环模式：** 到达 B 点自动暂停。

**自动缩放：** 设置 A/B 后自动缩放图表到 A-B 区域，清除后恢复全时间轴。

**图表同步：** A/B 标记跟随图表缩放/平移自动更新位置。

---

## 五、语谱图增强

语谱图支持独立的时间轴缩放和双向同步。

| 操作 | 方式 |
|------|------|
| 时间轴缩放 | 普通滚轮（以鼠标位置为中心） |
| 频率轴缩放 | `Ctrl + 滚轮`（以鼠标位置为中心） |
| 频率轴平移 | `Ctrl + 拖拽` |
| 重置频率范围 | 双击 Y 轴区域 |

**双向同步：** 图表和语谱图的时间轴保持同步。在任一面板缩放/平移，另一个自动跟随。

---

## 六、音频降噪

对音频进行频谱门控降噪，改善语谱图和音量图的信噪比。

| 操作 | 方式 |
|------|------|
| 调整降噪强度 | 语谱图标题栏的"降噪"滑块（0.0 ~ 2.0） |
| 应用降噪 | 点击"应用"按钮，自动触发音频重新分析 |

**降噪算法：** 基于频谱门控（spectral gating），使用纯 numpy 实现，无需额外依赖。

**强度说明：** 0.0 = 关闭，1.0 = 标准，2.0 = 强降噪。调整后需重新分析音频才能生效。

---

## 七、截图叠加

截图叠加用于截取某一帧画面或从外部加载图片，与当前视频进行半透明叠加对比。

### 从外部图片加载

除了截取当前视频帧，还可以从外部图片文件加载叠加层：

菜单 `文件 → 加载图片为叠加` → 选择图片文件（PNG/JPG/BMP/TIFF）→ 图片出现在截图叠加缩略图中 → 可编辑亮度/对比度/透明度 → 点击放置叠加到视频

### 工具栏三个按钮

| 按钮 | 功能 |
|------|------|
| **截取** | 截取当前视频帧，截取后左侧出现缩略图窗口 |
| **编辑** | 打开或关闭缩略图窗口 |
| **放置** | 将叠加应用到视频画面（激活时按钮变为橙色背景，再次点击取消叠加） |

三个按钮启用后显示橙色边框，方便辨识当前可操作状态。

### 缩略图窗口

截取帧后，缩略图窗口自动出现在视频画面左侧：

| 状态 | 说明 |
|------|------|
| 缩略图模式 | 小窗口预览截取帧，底部提示"▶ Edit" |
| 展开编辑面板 | 点击缩略图展开，显示大图预览 + 滑块 + 功能按钮 |
| 折叠回缩略图 | 编辑面板中点击预览图 |

### 编辑面板

展开编辑面板后，可以进行以下操作：

| 控件 | 功能 |
|------|------|
| 打勾按钮 | 将叠加应用到视频画面（等同于工具栏"放置"按钮） |
| 垃圾桶按钮 | 清除截图，取消所有叠加 |
| 关闭按钮 | 折叠回缩略图模式 |
| 亮度滑块 | 调整叠加层亮度（-50 ~ +50） |
| 对比度滑块 | 调整叠加层对比度（-50 ~ +50） |
| 透明度滑块 | 调整叠加层透明度（0 = 完全不透明，100 = 完全透明） |

工具栏"放置"按钮和编辑面板内打勾按钮功能一致，互相同步。

---

## 八、时间戳提取

时间戳提取用于固定视频中的一个区域，将其画面实时显示在一个独立的浮动窗口中。

| 操作 | 方式 |
|------|------|
| 创建 | 按住 `Ctrl+Shift` + 左键在视频上拖拽选择区域 |
| 关闭 | 关闭浮动窗口即可 |

浮动窗口始终显示在最上层，方便在操作其他功能时持续观察某个区域的实时画面。

---

## 九、时间设置

时间设置用于将图表时间轴与真实世界时间对应，适用于监控录像等需要对应真实时间的场景。

| 操作 | 方式 |
|------|------|
| 设置 | 点击工具栏 **"设置时间"**，输入当前视频对应的真实时间（HH:MM:SS 格式） |
| 效果 | 图表所有时间标签更新为输入的真实时间，青色光标上方也显示绝对时间 |

例如：视频播放到 `00:05:00` 时，输入 `10:30:00`，图表时间轴会从 `10:25:00` 开始显示。

---

## 十、轴标签

### 时间轴刻度

图表底部自动显示时间轴刻度尺，包含：

- **起止时间标注**：左右两端显示加粗的起止时间
- **主刻度线**：每个时间标签位置一条粗线
- **次刻度线**：主刻度之间均匀分布 4 条细线，提高可读性
- **时间步长自动选择**：根据可见时间范围自动调整标签密度（1秒 ~ 6小时）

右键图表区域可切换刻度的显示/隐藏。

### 光标时间显示

青色竖线（光标）上方自动显示当前时间，深色背景确保清晰可读。如果设置了时间轴，显示的是绝对时间。

### 用户自定义标签

| 操作 | 方式 |
|------|------|
| 添加标签 | 双击图表区域，输入文字，选择颜色，不选择则随机赋色（6 种预设色：红、蓝、绿、橙、黄、紫） |
| 跳转 | 单击标签，视频自动跳转到标签所在的时间位置 |
| 锚定 | 标签始终锚定在添加时的时间位置，缩放/平移图表后自动跟随 |

### 图表操作

| 操作 | 方式 |
|------|------|
| 缩放时间轴 | 鼠标滚轮（以光标位置为中心缩放） |
| 平移时间轴 | 按住鼠标中键拖拽，或 `Shift + 左键` 拖拽 |
| 恢复全局视图 | 双击图表区域 |
| 跳转视频 | 点击/拖拽青色竖线 |
| 右键菜单 | 全部适应 / X 轴适应 / 刻度开关 / Y 轴自动范围开关 |

---

## 附录

### 键盘快捷键一览

| 按键 | 功能 |
|------|------|
| `空格` / `k` | 播放 / 暂停 |
| `←` / `→` | 后退一帧 / 前进一帧 |
| `↑` / `↓` | 音量 +5 / -5 |
| `c` / `l` | 加速一档 |
| `x` / `j` | 减速一档 |
| `z` | 恢复 1x 倍速 |
| `n` | 在当前播放位置添加标签 |
| `A` | 设置 A 点（A/B 区域播放） |
| `B` | 设置 B 点（A/B 区域播放） |
| `L` | 切换 A/B 循环开/关 |
| `P` | 进入多边形模式 |
| `G` | 进入辅助线模式 |
| `Esc` | 关闭放大镜 / 退出当前模式 |
| `Delete` | 删除选中的 ROI / 辅助线 |
| `Ctrl+O` | 打开视频文件 |
| `Ctrl+S` | 保存分析结果 |

> 在程序中点击 **帮助 → 快捷键速查** 可查看完整快捷键列表（半透明弹窗）。

### 多边形模式

点击工具栏 **多边形** 按钮或按 `P` 键进入多边形模式：

| 操作 | 说明 |
|------|------|
| 单击 | 添加一个顶点 |
| 双击 | 闭合多边形（至少 3 个顶点） |
| 右键 | 取消当前绘制 |
| `Esc` | 退出多边形模式 |

多边形 ROI 支持亮度分析，计算多边形内部区域的平均亮度。支持移动整体和拖拽单个顶点调整形状。

### 辅助线模式

点击工具栏 **辅助线** 按钮或按 `G` 键进入辅助线模式：

| 操作 | 说明 |
|------|------|
| 左键拖拽 | 绘制辅助线 |
| `Shift` + 拖拽 | 约束为水平或垂直线 |
| 左键点击线体 | 选中并可移动整条线 |
| 左键拖拽端点 | 调整端点位置 |
| 右键点击 | 删除辅助线（无需先选中） |
| `Esc` | 退出辅助线模式 |

辅助线用于在视频画面上标注参考线，不参与亮度分析。

### 常见问题

| 问题 | 解决方法 |
|------|---------|
| 分析失败 "Python not found" | 运行 `setup_python_deps.bat` 安装 Python |
| 分析失败 "opencv not found" | 运行 `setup_python_deps.bat` 安装依赖 |
| 打开视频黑屏 | 确认视频格式支持，不支持的格式先转为 MP4 |
| DAV 格式无法打开 | DAV 是大华私有格式，请先转为 MP4 |
| 截图叠加不显示 | 需先点"截取"，再点"放置"才叠加到视频 |
| 放大镜内 ROI 不生效 | 直接在放大镜视图内操作 ROI |
| VLA 文件打不开 | 版本不兼容，v0.2 的 VLA 无法在 v0.1 打开 |
